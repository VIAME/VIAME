# Lite branch status ledger

Update after every task (see AGENT_GUIDE.md). Status values: `todo`,
`in-progress`, `done`, `blocked (reason)`, `skipped (reason)`. Put the commit
hash in the Commit column and anything that deviated from the task text in
Notes.

## Current position

- Phase: **P7 and P8 are done.** VXL, FFmpeg and Eigen are gone from kwiver,
  fletch and VIAME (P3, P4, P6), video is python on PyAV, the kwiver
  submodule is gone, OpenCV's codecs are replaced (P7-T02) and `image_ops`
  covers the rest of what it was used for (P7-T03). Phase 8 replaced
  kwiver's infrastructure: the registry is static and `plugin_loader` is
  `registry`, python packages declare their plugins instead of importing
  them, and `viame runner --help` costs 0.39 s against 2.72 s. The one piece
  of P8 still open is the fold of the `viame_<name>` targets into a single
  `libviame`, which is entangled with phases 1 and 2
- Next task: **phase 1 is nearly done: the superbuild is deleted.** One
  configure builds VIAME. T01 to T06 are done, T07 is done but for flipping
  `VIAME_INSTALL_PYTHON_DEPS` on, and T02's remaining option removals went
  with the superbuild. What is left of the phase is T08's tail (the
  `plugins/matlab` question, open decision 4), T09 (docker and CI, which
  still reference options and projects that no longer exist) and T10 (the
  DIVE smoke test). Previously: T01, T04, T05 are done, T02 is
  nine tenths done and T07 is most of the way; the top-level CMakeLists is
  1354 lines down to 200, with `viame_options.cmake`,
  `viame_dependencies.cmake`, `viame_python_deps.cmake` and
  `viame_project.cmake` beside it, and `packages/fletch` is deleted along
  with the four options that only ever told it what to build. What proved it
  safe was building a complete VIAME into a prefix fletch had never touched
  and running it -- 151 algorithms, 101 processes, all 31 applets, and the
  only things missing were python packages. **What is left of the phase** is
  the other half of P1-T07: the `viame_python_forks` target that builds
  `packages/pytorch-libs/*`, the `python/patches/apply.py` step, and turning
  `VIAME_INSTALL_PYTHON_DEPS` on -- which upgrades 56 packages on an install
  built the old way and wants its own verification pass. When those land,
  P1-T06 deletes the superbuild and the rest of T02's option list goes with
  it
- `library/` has three OpenCV includes left, all of them waiting on a
  decision rather than on work; see "Where phase 7's OpenCV removal stands".
  `plugins/` has three: `plot_metrics`, which is a rewrite rather than a
  port, and open decision 6's two model classifiers -- plus
  `plugins/darknet`, which is P7-T08's. **`plugins/core` is clear**
- Last clean-configure build verified: 2026-09-13, after phase 1's
  restructure, in an empty `build/clean-build` with no `fletch_DIR`, no
  `OpenCV_DIR` and none of the removed options in the cache. It compiles to
  100 per cent and **no target's flags, link line or rpath names the
  reference superbuild at all**. It also found one latent bug a warm tree
  cannot: `install_egg_info` ran `setup.py egg_info --egg-base` on a
  directory nothing had created, and only worked because an earlier run had
  left it there. Before that, 2026-09-11 after P7-T06. A fresh
  `build/clean-build` configured and compiled the whole tree with **no
  errors**, which is the check a warm tree cannot give: a header that is only
  found because an old copy sits in the install prefix, or a target whose
  dependency is satisfied by a stale object, shows up here and nowhere else.
  The incremental tree is at **305 of 305** -- the jump from 29 is
  `kwiver_discover_gtests` finally labelling what it discovers, see finding
  1.20. The command is `ctest -L "BASELINE|GOLDEN|UNIT|CRITICAL|CORE"`
- **The compatibility baseline checks python algorithms' config for the
  first time.** Three separate defects, in three layers, each enough on its
  own to make a python implementation's configuration invisible to
  `registry-dump`: the pybind11 binding passed a non-copyable `config_block`
  by reference, P8-T10's lazy declarations refused to import, and
  `register_vital_algorithm` supplied a `get_default_config` that set
  nothing. `compare_registry.py` skips an entry it cannot introspect, so the
  effect was that an algorithm stopped having its keys checked **at the
  moment it was ported from C++ to python** -- 143 keys across the 24
  implementations phases 3 to 7 moved. All three are fixed, the baseline test
  runs `registry-dump --introspect`, and all 143 keys and defaults match the
  C++ originals exactly. See finding 1.35
- Reference machine: local workstation, CUDA 12.6, cuDNN 9.12, Ubuntu
  (kernel 6.8), python 3.10.12, gcc default, 16 cores

### Build arrangement until phase 1 finishes

P1-T05 landed early, so this is one configure now. What is still missing from
phase 1 is the superbuild's replacement: the `lite` checkout builds only the
VIAME project itself and takes fletch, darknet and the python environment
from an existing `main` superbuild of the same commit.

| What | Where |
|---|---|
| Source | `~/Dev/viame-lite/src` (this checkout, branch `lite`) |
| Build | `~/Dev/viame-lite/build/merged-build`, and `build/clean-build` for the from-scratch check |
| kwiver build | none: P1-T05 made kwiver a subdirectory and P5-T05 removed it entirely. `build/kwiver-build`, `build/viame-build` and `build/kwiver-cache.cmake` are dead |
| Install | `~/Dev/viame-lite/build/install` (seeded by copying the reference install) |
| Initial cache | `~/Dev/viame-lite/build/lite-cache.cmake`, mirroring every `VIAME_*` setting of the reference build |
| fletch, darknet | **fletch is deleted.** `packages/fletch` and `add_project_fletch.cmake` are gone; darknet is `third_party/darknet`. `build/clean-install` is a complete install built into a prefix fletch never touched |
| kwiver | gone since P5-T05; `library/`, `python/` and `tools/` are what it was |

```
cmake -S src -B build/merged-build -C build/lite-cache.cmake  # PATH must have nvcc
cmake --build build/merged-build -j16 && cmake --install build/merged-build
ctest --test-dir build/merged-build -L BASELINE
```

Before P1-T05 this was two configures, kwiver's first, with the flag phase 3
drove (`KWIVER_ENABLE_VXL`) in `build/kwiver-cache.cmake`. VIAME's top-level
sets every `KWIVER_ENABLE_*` itself now.

`packages/downloads` holds symlinks to the reference checkout's downloads so
that model packs are not fetched twice. The `fletch` submodule is checked out
for `CMake/FindCUDNN.cmake`, and `kwiver` is checked out and built.

## Tasks

| ID | Task | Depends | Status | Commit | Notes |
|---|---|---|---|---|---|
| **Phase 0: baseline and compatibility contract** | `phase-00-baseline.md` | | | | |
| P0-T01 | Create the `lite` branch | - | done | 2547f6cb8 | `design/` committed on the new branch |
| P0-T02 | `registry-dump` applet | P0-T01 | done | 46669f626 | `plugin_map()` is protected, so the applet goes through `plugin_manager_internal`. 58 python-registered algorithms cannot report their config (the pybind trampoline returns the non-copyable `config_block` by copy) and are recorded with an `error` field instead, so their names are covered but not their defaults. Algorithm descriptions end in a registration-ordered option list, which python import order perturbs; the writer sorts it so two runs are byte identical |
| P0-T03 | `pipe-check` applet | P0-T01 | done | bb08e75a0 | Walks the install tree (`configs/pipelines`, `configs/add-ons`, `examples`) instead of extracting the add-on zips: the build already unpacks add-on pipelines into the install, so this covers them with no zip handling. A pipe names an implementation without its interface, so resolution means "registered under some interface"; that also matches nested non-algorithm keys such as `vxl_average:type = window`, which read as unresolved. Stable, so changes are still caught |
| P0-T04 | Baseline files and compare scripts | P0-T02, P0-T03 | done | 6711a97bc | Baseline is the CUDA build only, not a GPU/CPU union: see the new P0-T06. 293 files checked, 34 failing today, which is more than the two stale names the task text expected; the failures are recorded as the baseline so a fix is noticed too |
| P0-T05 | Wire baseline checks into ctest | P0-T04 | done | a6c4a6429 | `ctest -L BASELINE` passes, 17 s for both |
| P0-T06 | Merge a CPU-build registry dump into the baseline | P0-T05 | done | this commit | **The premise does not hold, and that is the result.** The task assumed a CPU build provides a different set of names, so the baseline should become a union with each entry tagged `[gpu]`, `[cpu]` or both and `compare_registry.py` given a `--config` switch. Nothing in VIAME registers conditionally on CUDA: there is not one `#if` in any `register_algorithms.cxx` or `register_processes.cxx`, and the only CUDA macros in the tree are compile definitions inside `plugins/darknet` and the vendored `remax` extension, neither of which gates a factory. The two pipelines `configs/pipelines/CMakeLists.txt` swaps between configurations -- `common_default_descriptor` and `query_augment_image` -- differ by a single config **value**, `gpu_list` 0 against None, and name the same processes and implementations. So there is no union to record and no switch to add; a per-entry `configs` field would be machinery for a distinction that does not exist. Checked as well as read: the registry dumped with `CUDA_VISIBLE_DEVICES=` empty is **identical**, 642 registrations either way, nothing lost and nothing gained. That is now `baseline:registry:no_gpu` and `baseline:pipes:no_gpu`, two more BASELINE tests that run on every build and cost nine seconds between them. What they guard is the thing that could actually break CPU support and is invisible to the CUDA-build baseline: a registration put behind a CUDA guard, or a **python implementation whose module probes the device while importing**, which no build flag controls and which would make a CPU install quietly provide less than the baseline promises. A second build was configured and abandoned rather than forced, and why is worth recording: `VIAME_INSTALL_PREFIX` is forced equal to the install directory, and `plugins/svm` and others resolve their dependencies out of it, so a second configuration cannot be built without first replicating the whole populated prefix. That coupling is what P1's single build removes |
| **Phase 1: single build with code in place** | `phase-01-single-build.md` | | | | |
| P1-T01 | Inventory the normal-build branch | P0-T05 | done | this commit | The `else()` branch is `cmake/viame_project.cmake`, 353 lines, lifted out unchanged -- `include()` not `add_subdirectory()`, so the variable scope is what it was inside the branch, and the only edit is the indent. The top-level file is 1354 lines down to 1022, and the branch is now three lines. **The inventory, and it is mostly a list of things nobody reads.** The superbuild handed the inner configure its arguments through `FormatPassdowns`, which forwards **every cache variable whose name starts with a prefix** -- `CUDA`, `CUDNN`, `VIAME_ENABLE`, `VIAME_BUILD`, and five more. On this machine that is 399 variables, and **306 of them are read by nothing in the project build**; the great majority are `FindCUDA`'s own cache entries (`CUDA_BUILD_CUBIN`, `CUDA_HOST_COMPILATION_CPP`, each with its `-ADVANCED` twin), swept up because they begin with the right four letters. **What actually crosses the boundary, by group.** *Dependency locations:* `fletch_DIR` has two readers left -- an `if( IS_DIRECTORY )` guard at the top of the extracted branch and a line in `viame-config-install.cmake.in` -- and `OpenCV_DIR`, `kwiver_DIR` and `Darknet_DIR` have **none**: P7 took OpenCV out of the C++, P5-T05 dissolved the kwiver submodule and darknet is `third_party/` now, so three of the four handles the task text names are already dead. `VIAME_ARGS_darknet` is expanded in `add_project_viame.cmake` and set nowhere. *Superbuild-computed paths:* `VIAME_BUILD_PREFIX`, `VIAME_BUILD_INSTALL_PREFIX`, `VIAME_INSTALL_PREFIX`, `VIAME_BUILD_PLUGINS_DIR`, `VIAME_BUILD_FLETCH_DIR`, `VIAME_BUILD_KWIVER_DIR`, `VIAME_PACKAGES_DIR` -- all seven are computed in the top-level file above the branch, not passed in, so a single build keeps them by keeping those lines. *CUDA and cuDNN:* `CUDA_TOOLKIT_ROOT_DIR`, `CUDA_NVCC_EXECUTABLE`, `CUDA_VERSION`, `CUDA_ARCHITECTURES`, `CUDA_cublas_LIBRARY`, `CUDA_cublas_device_LIBRARY`, `CUDNN_ROOT_DIR`, `CUDNN_LIBRARY`, `CUDNN_LIBRARIES`, `CUDNN_INCLUDE_DIR`, `CUDNN_INCLUDE_FILE` -- eleven, every one of them read by the detection block in the top-level file that P1-T02 keeps; `CUDNN_VERSION` is passed and read by nothing. *Python:* `Python_EXECUTABLE` and `VIAME_PYTHON_VERSION` are read; `PYTHON_EXECUTABLE`, `KWIVER_PYTHON_MAJOR_VERSION` and `KWIVER_PYTHON_USE_SYS_PATH` are passed and read by nothing -- the last two were kwiver's, and kwiver is not a separate configure any more. *Other:* `BUILD_SHARED_LIBS`, `LIB_SUFFIX`, `VIAME_OPENCV_VERSION`, `VIAME_CREATE_INSTALLER`, `VIAME_FIXUP_BUNDLE`, `Matlab_ROOT_DIR` are read; `VIAME_IN_SUPERBUILD` and `SEAGIS_DIR` are not. **So the passdown list P1-T02 has to reproduce is eleven CUDA/cuDNN names, two python names, `fletch_DIR`, and the VIAME options themselves** -- and the options stop being a passdown at all in a single build, because they are declared by the one configure that reads them. |
| P1-T02 | New top-level CMakeLists and options file | P1-T01 | in-progress | this commit | **Half done, and the half that is left is blocked on P1-T06, not on judgement.** The options are `cmake/viame_options.cmake` and the top-level file is 1354 lines down to 771. Nine of the ten `VIAME_ENABLE_*` names `lite-build-system.md` section 2 removes in P1 are gone with the code they guarded: `VIVIA`, `SEAL` and `KEYPOINT` were three separate repositories the superbuild cloned (VIVIA had been a fatal error since P3 took VXL), `TENSORFLOW` and `TENSORRT` two inference backends no shipped pipeline selects, `GDAL` and `WIN32GUI` only ever told fletch what to build, and `KWIVER` and `VIAME_PLUGINS` were "build one superbuild package without VIAME" in a tree that has no other packages -- one was already a fatal error without the other. `plugins/tensorflow`, five `add_project_*.cmake` files and the example and pipeline gating went with them. Nothing installed changed: all nine were OFF in the reference configuration, which is also why this could not have regressed the baseline and did not. **`VIAME_ENABLE_MATLAB` stays**, and that is deliberate: it is on the same list but it is also open decision 4, which `lite-plan.md` *proposes* rather than takes, and `plugins/matlab` is two working bridges rather than a build-system artefact. Every open decision so far has been the user's; this one is left for them. **What is left of T02** is the `VIAME_BUILD_*` half of the list -- `FLETCH_DIR`, `KWIVER_DIR`, `PLUGINS_DIR`, `FORCE_REBUILD`, `CORE_IMAGE_LIBS`, `PYTHON_FROM_SOURCE`, `PYTORCH_FROM_SOURCE`, `TORCHVISION_FROM_SOURCE`, `LIMIT_NINJA`, `PACKAGING_CONT` -- plus `VIAME_FIXUP_BUNDLE`, `VIAME_OPENCV_VERSION` and `VIAME_FORCE_CUDA_CSTD98`. Every one of them is read only by `add_project_fletch.cmake`, `add_project_viame.cmake` or the packaging step, so they go when the superbuild does, which is P1-T06; deleting them first would mean editing files that are about to be deleted. One divergence to record: `VIAME_PYTHON_SYMLINK` is on the removal list but P8-T08 kept it, and `cmake/viame/viame-python.cmake` reads it -- it is a live developer convenience, not superbuild machinery. |
| P1-T03 | `third_party/` for the small libraries | P1-T02 | in-progress | this commit | Pulled forward out of order, because it does not need P1-T02's new top-level CMakeLists and every package that moves is one fletch stops having to build. Three done: **libsvm** (2 files), **TinyXML** (4 files) and **pybind11** (28 of 36 headers). Minimal by instruction -- only what VIAME compiles or includes, never a distribution -- and `third_party/README.md` records what was left behind in each case, because "why is this file not here" is the question a later reader has. The pybind11 set is the transitive closure of the eleven headers VIAME includes, plus every `detail/` header since two are reached only conditionally; what that drops is the whole `eigen/` subtree, for a library phase 6 removed, and four feature headers nothing includes. Proved sufficient by deleting fletch's copy from the install prefix and rebuilding clean. `find_package( pybind11 )` is gone from four CMakeLists; the `pybind11::pybind11`, `::module` and `::embed` targets are defined in `third_party/pybind11/CMakeLists.txt` instead, which is all the tree ever used -- nothing calls `pybind11_add_module`. libsvm and TinyXML are now **static and linked in**, so two shared libraries left the runtime set. libsvm had been found in the install prefix with `NO_DEFAULT_PATH`, one of the couplings that made a second build configuration impossible to configure (P0-T06). **All of it is done now.** **miniz** replaces ZLib -- measured, since the instruction was whichever is smaller: +61 KB in `libviame_core` against -121 KB of `libz.so`, and the archive APIs stripped out take miniz from 105 KB of object code to 48 KB. **darknet** is vendored **inference only**, 18 MB to 2 MB, which also removed its trainer: the trainer shelled out to the `darknet` executable and that executable is built from the 17 command line drivers nobody wanted. **GoogleTest** is fetched and built when tests are on, rather than found -- `find_package( GTest REQUIRED )` resolved through fletch's prefix and was the last thing in VIAME's own build that needed fletch. **Checked rather than claimed: VIAME now configures and generates with `fletch_DIR` pointed at a directory that does not exist.** Getting there needed the vendored statics in the export set, because `export()` names every dependency of an exported target before a generator expression can hide one, and `plugins/svm` stopped taking `<svm.h>` from the install prefix -- where fletch's copy also sits, so the vendored header with VIAME's own kernels was being shadowed by the one it replaced |
| P1-T04 | `viame_dependencies.cmake` | P1-T03 | done | this commit | `cmake/viame_dependencies.cmake`, and the striking thing is what is **not** in it. The task text asks for `find_package` of OpenCV, FFMPEG, Python3, ZLIB, Threads, OpenMP and optional CUDAToolkit; by the time phase 1 arrived, six of the seven were gone with the phases that removed the dependency, and fletch with them. What the file holds is the compiler check, CUDA and cuDNN detection, python detection and its version checks, the pytorch version matrix, and two optional vendor libraries. **Measured rather than asserted**: every VIAME library in a finished Linux install has exactly these `NEEDED` entries outside the tree -- `libc`, `libstdc++`, `libgcc_s`, `libm`, `libpthread`, `librt`, `libdl`, `libgomp`, `libpython3.10`, and CUDA's own when it is enabled. `libz` and `libexpat` show up under `ldd` but neither is direct: both arrive through libpython. Nothing links zlib, expat, OpenCV, FFmpeg, Eigen, Boost or Qt. The apt, dnf and brew lists in `examples/building_from_source/README.rst` were rewritten from that measurement -- they had been fletch's build-dependency list (libgtk2, libxml2, lapack, mesa, openssl, curl-devel) and are now a compiler, CMake, git, and python with headers. The one thing the task asks for that is **not** done is the docs path: the text says `docs/manual/building.md`, and the tree has `docs/manual/sections/building_from_source.rst`, which is one line including the example's README, so the README is where the lists went |
| P1-T05 | Kwiver as a subdirectory | P1-T04 | in-progress | this commit | Pulled forward out of order: P5-T05 cannot finish without it. Only the `add_subdirectory` half of the task is done -- the superbuild path in `cmake/add_project_kwiver.cmake` is untouched, and P1-T02 to P1-T04 (new top-level CMakeLists, `third_party/` targets, `viame_dependencies.cmake`) are still todo -- so this row stays open until phase 1 proper. One `cmake` and one `make` now build kwiver, `library/`, `python/kwiver`, `plugins/` and `tools/` together; `build/merged-build` is the build tree and `build/kwiver-build` plus `build/viame-build` are no longer needed. Six changes in kwiver and four in VIAME, each of them about the difference between being the project and being a subdirectory of one. Three were not obvious. Kwiver's warning flags reach VIAME through the global `kwiver_warnings` property, so VIAME compiled with `-Werror=non-virtual-dtor` and `-Werror=zero-as-null-pointer-constant` for the first time and its own code and TinyXML's header both failed; VIAME clears the property before reading it. The python package the bindings install into came from `CMAKE_PROJECT_NAME`, so the whole `kwiver.vital` tree installed a second time as `viame.vital` and the duplicate pybind11 modules aborted the registry dump. And `version.h`, which `kwiver_configure_file` writes at build time, was being copied at configure time by P5-T02's republishing macro -- it had only ever worked because the file was left over from the previous build. 25 of 25 tests pass |
| P1-T06 | Plugins, tools, tests inside the same build | P1-T05 | done | this commit | **Done. The superbuild is deleted.** DIVE was the last thing in it, and stays exactly what it was: its own repository, carried as the `packages/dive` submodule, built from there or downloaded as a release. `cmake/viame_dive.cmake` is `add_project_dive.cmake` with one thing changed -- everything that decided *what* to do ran at configure time and is untouched; what was an `ExternalProject_Add`, which needs a superbuild to live in, is a custom command, which does not. Both paths exercised: the binary path fetched, extracted and installed DIVE, and the source path's node hunt rejected the system's node 18 as below DIVE's minimum of 22, found nvm's 24.16.0 and the npm beside it. The Electron build itself was not run -- this checkout has never populated that submodule. Deleted with the superbuild: `add_project_{viame,pytorch,python_deps,onnx}`, `common_args`, `build_package`, `installation_blacklist`, `setup_ctest`, `custom_install_viame`, and ten options that only ever steered subprojects, plus a Windows path-length check about nested build directories that are no longer nested. **The top-level `CMakeLists.txt` is 1354 lines down to 143.** The `add_subdirectory` half was done by P1-T05. The delete half is **down to one item**. Gone so far: `add_project_fletch`, `custom_install_fletch`, `add_project_{vivia,seal_tk,keypointgui,tensorflow,tensorrt}`, `linux-remove-duplicate-cvs`, `old_version_checks`, and `add_project_onnx`, whose entire body was a URL and a copy -- it is `cmake/viame_fetch_archive.cmake` now, tested against the real URL, because the onnxruntime C++ libraries are a build dependency of the mmdeploy *fork* and belong with the forks. `add_project_{python_deps,pytorch}` are superseded by `viame_python_deps.cmake` and `viame_python_forks.cmake`. **The one thing left is DIVE, and it stays where it is.** The user's call: DIVE remains its own repository, referenced as a git submodule, and is not integrated into VIAME. A submodule is exactly the non-integrated way to reference another project, so `packages/dive` and `add_project_dive.cmake` are untouched. What is left is not *whether* VIAME builds DIVE but *where from*: `add_project_dive.cmake` is an `ExternalProject_Add` and so needs a superbuild to live in. Moving it to an `add_custom_command` in the project build, keeping both its paths -- the prebuilt fetch and the node/npm Electron build from the submodule -- is the last thing between here and deleting the superbuild. It is 312 lines, 230 of them the node hunt and the Electron build, and it cannot be verified on a machine with no node toolchain, so it wants its own pass. When it moves, the superbuild branch, `common_args`, `add_project_viame`, `build_package`, `setup_ctest`, `installation_blacklist` and `custom_install_viame` go with it, and the `VIAME_BUILD_*` half of T02's option list with them |
| P1-T07 | Python dependency step | P1-T06 | in-progress | this commit |  **The rest of it landed.** `cmake/viame_python_forks.cmake` builds each enabled package in `packages/pytorch-libs` with `pip wheel --no-build-isolation --no-deps` and installs it `--no-deps` -- reusing `custom_build_python_dep.cmake` rather than reimplementing its source-hash guard, which for mmcv is twenty minutes against nothing. Two forks stop being built and neither is a loss: `packages/python-utils/pyav`'s patched `setup.py` exists to build PyAV against a specific FFmpeg directory, which was fletch's, and P4 already moved VIAME to the `av` wheel that carries its own; `torchvision` carries no VIAME patch and the torch index publishes a matching `+cuXXX` build. `python/patches/apply.py` carries the three post-install edits `custom_install_viame.cmake` made -- `torch.load`'s `weights_only` default and ubelt's removal of `ensure_unicode` -- and unlike `ReplaceStringInFile`, which substituted in silence, it reports what it did and **fails when a patch matches nothing and its result is not already there**. Checked both ways: seven already in place on the working install, and a synthetic tree where the packages have moved on fails with the file named. **Not verified end to end**: this checkout has never populated the `packages/pytorch-libs` submodules -- it uses the reference build's installed forks -- so the fork target is checked by its generated command rather than by a run; it says so clearly when a submodule is empty and the option is on. **What is left**: flipping `VIAME_INSTALL_PYTHON_DEPS` on, which upgrades 56 packages on an install built the old way.`python/requirements/{base,cuda12,cuda13,cpu,forks,learn,colmap,test}.in` and their locks, plus `cmake/viame_python_deps.cmake`. The design's `sam.in` and `stereo.in` are not there: what those options add is `huggingface_hub`, `safetensors` and `timm`, three small packages that three of the five options needing them turn on by default, so they are in `base.in` instead of two files that would each have held the same three lines. **What the lock is worth, measured**: of 185 locked packages, **56 are pinned to a different version** than the one pip happened to choose when the working install was built -- two builds of the same commit a month apart did not agree, and there was no file to diff. **`forks.in` is the part the task text points at and the part that was actually wrong.** `packages/pytorch-libs/*` are source builds and the superbuild installed each one's wheel *without* `--no-deps`, so pip resolved its requirements freely; the list is taken from each fork's own metadata (`pip show`) rather than from reading a setup.py. **Three of the five exclusions are one bug**: `ultralytics` requires `opencv-python`, `albumentations` and `mmengine` require `opencv-python-headless`, `pylabel` requires `opencv-python`, and each installs a `cv2` into the same directory as VIAME's contrib wheel -- which is what `linux-remove-duplicate-cvs.cmake` existed to paper over. `lite-install-size.md` section 4's two checks were done before excluding `triton` and `decord`: nothing in VIAME or in any submodule this build compiles imports `decord`, and sam3's only `triton` use is an EDT kernel already guarded with an OpenCV CPU fallback. **A side effect worth having**: the locks drop 28 packages that are in the install by accident -- `peft`, `accelerate`, `open-clip-torch`, `fairscale`, `polygraphy`, `xmltodict`, and `pylabel`, which drags a twenty-package Jupyter notebook widget stack behind it. Not one is imported or required by anything; `motmetrics` was the only one of the set VIAME actually uses and it is declared now. **Not done**: `VIAME_INSTALL_PYTHON_DEPS` defaults OFF rather than ON, because the first run upgrades those 56 packages and that wants its own verification pass rather than arriving as a side effect of the next `make` -- the command it generates is verified with `pip --dry-run`. The `viame_python_forks` target and the `python/patches/apply.py` step are still the superbuild's, so P1-T06 cannot delete it yet |
| P1-T08 | Remove dead trees and submodules | P1-T07 | done | this commit | **cv2 is a wheel, and fletch has no users left.** `add_project_python_deps.cmake` had deliberately omitted `opencv-python-headless` because fletch's OpenCV build installs a `cv2` module *and* two fake `opencv_python*.dist-info` directories so pip believes the wheel is already there -- with `linux-remove-duplicate-cvs.cmake` deleting the real wheel if anyone installed one. Both are gone and the wheel is in the basic dependency list, unpinned. **contrib, and latest, both on evidence.** Measured against every `cv2.` symbol VIAME's python names, 218 of them: the contrib wheel is missing exactly the same five as the 4.9.0 build it replaces, all OpenCV 2 spellings in code that cannot reach them; the plain wheel is missing those five plus `ximgproc` (WLS disparity) and `xfeatures2d`. And 5.0.0.93's golden failures are a strict **subset** of 4.14.0.94's -- 4.14 additionally breaks `ocv_enhancer:saturation_1_20` and three filter pipelines -- so the most recent is also the better one. **What it cost, measured.** Every numeric difference from OpenCV 4.9 to 5.0 is in the last bits: fundamental matrices 1.0e-15, homographies 1.7e-7, SIFT keypoints 3.1e-5 (one float32 ULP), calibration intrinsics about 1e-6 relative, and one SIFT feature in 124 moving far enough to fall out of an exact set match. Eleven golden cases carry a tolerance now, each at the measured magnitude with the reason beside it, and the calibrations still pass `check_calibration_truth` untouched -- they are compared against the rig the views were *rendered* through, so the calibration is as correct as it was and only written to different digits. **What is genuinely lost is SURF.** No `opencv-python*` wheel is built with `OPENCV_ENABLE_NONFREE`, so `xfeatures2d.SURF_create` raises "this algorithm is patented and is excluded". `ocv_SURF` stays registered and says so, pointing at `ocv_SIFT`; its two unit tests and seven golden cases skip when cv2 has no SURF rather than being deleted, so a site that builds its own OpenCV still runs them. |  **Started out of order, for the eight submodules the P1-T02 option removals left with no reader.** `packages/{vivia,seal-tk,tensorrt,tensorflow-libs/models,itk-modules/*}` and `packages/pytorch-libs/{torch2rt,mmdet-to-tensorrt}` are gone from `.gitmodules` and the tree, with the last two branches of `add_project_pytorch.cmake` that named them and the two keypoint-GUI launcher scripts in `examples/registration_and_mosaicing`. `git grep` finds no reference to any of them outside `RELEASE_NOTES.md`, the Windows MSI staging scripts that P1-T08 also deletes, and one false positive -- `plugins/onnx/fast_foundation_stereo.py` names "tensorrt" as an *onnxruntime backend*, which is a runtime choice rather than the deleted submodule. **The rest of T08 waits on T07**: `packages/fletch` cannot go while cv2 comes from it, and `plugins/matlab` waits on open decision 4, which this task's own text says to take as removal -- worth noting that `lite-plan.md` still calls it a proposal, so the two documents disagree and the user should settle it  **The install blacklist turned out not to be needed.** `lite-install-size.md` section 3 rows 1 and 7 -- 434 MB of static `.a` archives in `lib/`, plus `bin/postgres`, the sqlite CLI and the GeographicLib and libtiff tools -- were to be suppressed by a blacklist in `cmake/viame_install.cmake`. Measured on the finished install instead: of the 70 `.a` files in `lib/`, **66 are not installed by VIAME at all**. They are fletch's, left in a prefix VIAME shares with it, and `make install` only ever adds -- finding 1.30 again. VIAME installs four, 12 MB between them, all `third_party/`, and installs none of the named binaries. The 449 MB is already gone because fletch is, and a blacklist now would only delete our own vendored archives; whether those are worth installing is a P10 packaging question, not a size one. `git submodule status` lists 19 -- dive, pyav and seventeen under `pytorch-libs` -- which is shorter than the done-when asks for, since kwiver and darknet are not submodules any more. |
| P1-T09 | Docker and CI for the single build | P1-T08 | done | this commit | **Done.** Both workflows were broken by the superbuild's removal: each keyed a cache on `sha256sum cmake/add_project_fletch.cmake`, a hard failure once the file went, while the `find packages/fletch` beside it had `2>/dev/null` and quietly yielded an empty hash instead. `build-ci.yml` is 450 lines down to 163 -- three staged caches and five sequential `--target` builds become configure, build, install, test, with ccache for what is worth caching, a Windows job of the same shape, and a job that resolves every lock and checks the exclusions hold. `build-release.yml` is 953 down to 579: its three Linux jobs -- Stage 1 (fletch), budgeted eight hours, Stage 2 (pytorch), Stage 3 (kwiver + viame) -- existed because fletch alone did not fit in a runner's time limit, and `build_server_rocky.sh` already did the whole build in one go, so the three stage scripts are deleted. **Verified as far as a machine without GitHub Actions can**: the Linux job's exact sequence was run locally into a throwaway prefix, and 409 of 409 UNIT and CORE tests pass on a build whose python came only from the committed locks. That run also corrected the job -- `-L BASELINE` cannot pass on a reduced-option build, because the baseline is the contract for the **full** option set and lists what pytorch and onnx register, so it fails `baseline:registry` and `baseline:pipes` by construction. The release build runs the full set, which is where that contract belongs |
| P1-T10 | DIVE smoke test | P1-T08 | blocked | | **Needs a person.** Every other check in this phase is something a machine runs; this one is "point DIVE desktop at `build/install`, open an image folder, run a detector, run a tracker, launch a short training, run scoring" -- a human driving a desktop GUI. The install it would be pointed at is built, and `install/dive` holds the desktop app |
| **Phase 2: functional library layout** | `phase-02-library-layout.md` | | | | |
| P2-T01 | CMake helpers (kwiver-backed) | P1-T10 | done | | The helper family is `cmake/viame/viame-*.cmake`, written in P8-T08 rather than as `cmake/viame_macros.cmake`; `viame_add_python_package` added here, and static registration is `viame_register_statically` rather than a dynamic module |
| P2-T02 | Generate the file map | P2-T01 | done | | `apply_file_map.py` (git mv, include rewriting, export-macro retargeting) and `check_file_map.py`, which compares the map against lite-library-layout.md §3 -- it found 50 files the generated map had put somewhere the plan does not; the document won |
| P2-T03 | Skeleton `library/` with empty targets | P2-T02 | partial | | `library/utilities` created; `segmentation`, `training` and `evaluation` still to come |
| P2-T04 | Move `utilities`, `video_io`, `file_io` | P2-T03 | done | | `utilities`, `video_io` and `file_io` moved, one at a time with a build and a test between. Three of `plugins/core`'s helpers went further down than lite-library-layout.md §3 first put them: `convert_polygons_to_mask` and `utilities_segmentation` are the polygon-and-mask geometry the CSV readers run and are `image_ops` now (which is why that target stopped being INTERFACE), and `utilities_target_clfr` is shared by four siblings and is `utilities`. The document's own DAG rule -- a library needing a sibling's helper moves the helper down -- decided all three; §3 was updated to match |
| P2-T05 | Move `image_processing`, `object_detectors`, `classifiers`, `segmentation` | P2-T04 | todo | | |
| P2-T06 | Move `object_trackers`, `descriptors`, `measurement` | P2-T05 | todo | | |
| P2-T07 | Move `training`, `evaluation`, `examples`, `tools` links | P2-T06 | todo | | |
| P2-T08 | Move remaining plugins and delete `plugins/` | P2-T07 | todo | | |
| P2-T09 | Relocate tests | P2-T08 | todo | | |
| P2-T10 | Docs and DIVE smoke | P2-T09 | todo | | |
| **Phase 3: drop VXL** | `phase-03-drop-vxl.md` | | | | |
| P3-T01 | Record VXL golden outputs | P0-T05 | done | 4b46563ba | Dependency changed from P2-T10 by the ordering decision. Recorded through the kwiver python bindings rather than through pipelines, which gives the exact array for every dtype instead of a re-encoded image; whole-pipeline recordings run the shipped `train_aug_*` pipelines, the only shipped pipelines that both use a vxl filter and write images |
| P3-T02 | `image_ops` v1 kernels | P0-T05 | done | 1205fc963 | Lands in `plugins/image_ops` for now; phase 2 moves it to `library/image_ops`. This commit carries the kernels the conversion, threshold and averaging filters need; the morphology, colour histogram, blur and white balance kernels land with P3-T04 and P3-T05, so each kernel arrives with the golden that checks it. Reproducing VXL exactly turned up two behaviours worth knowing: `vil_math_mean_over_planes` accumulates in the pixel type, so two uint8 planes of 100 and 200 average to 22, and the windowed averager subtracts the *newest* buffered frame rather than the oldest, so a full window is not a sliding mean. Both are pinned by unit tests |
| P3-T03 | `convert_image` (alias `vxl_convert_image`) | P3-T01, P3-T02 | done | 22eec3036 | Registered as `convert_image` only: the alias waits for P3-T10 to build kwiver with `KWIVER_ENABLE_VXL=OFF`, since `arrows/vxl` holds the name until then. Equivalence is checked instead by replaying every recorded `vxl_convert_image` case against `convert_image`, driven by the new `REPLACEMENTS` map in `tests/golden/cases.py`; all 14 variants match bit for bit on all three input types. `pending.json` does not exist yet, so nothing was removed from it |
| P3-T04 | `average_frames`, `threshold`, `morphology`, `color_commonality` | P3-T03 | done | a4069a51a | New names only, aliases still wait for VXL to be off. Two kernels added to `image_ops`: binary morphology and the colour commonality histogram. The structuring element and the morphology border rule were measured from the running implementation rather than read out of VXL sources, which is how the disk radius rule (strict `i*i + j*j < r*r`, so radius 1 is one pixel and 2 is a 3x3 square) and the border rule (the element is clipped, so eroding an all-true image leaves it all true) were pinned. `vil_threshold_above` turned out to be `>=`, not `>`: caught by the golden replay on the one input that has pixels exactly at the threshold, after a unit test had asserted the wrong rule. Two recorded paths are deliberate divergences, both uninitialised in VXL and unused by pipelines: `color_commonality` grid mode (tile regions built with corners in the wrong order) and `threshold` percentile on a multi-plane image |
| P3-T05 | `white_balance`, `vxl_enhancer`, `format_images_srm` | P3-T04 | done | 4ce488d49, 3e6c2bb0e | VIAME's own code is now VXL free and `plugins/vxl` is deleted. Deviations, all following the "used, nothing extra" rule: `vxl_white_balancing` was 682 lines of VXL-specific code with zero pipeline uses, so it went to `removed.json` rather than being ported. `vxl_enhancer` became an alias of `ocv_enhancer` after finding the two were already the same code at runtime: `plugins/vxl` and `plugins/opencv` each defined `viame::enhance_images` with identical mangled symbols, so the loader bound one definition for both factories and which one won depended on plugin load order. They matched on all 15 recorded cases. Two extra VXL users the plan does not mention were found and ported: `format_images_srm` (needed a bilinear resample kernel) and `convert_polys_to_mask`, which the VIAME CSV readers use for `poly_to_mask` and which used vgl polygon scan conversion; its `VIAME_ENABLE_VXL` guards are gone so the feature keeps working |
| P3-T06 | `core` image_io and unreferenced vxl names | P3-T05 | done | dfe480070 | Task text corrected first: aliasing `vxl` to `ocv` would have dropped all five config keys and changed `force_byte` and `auto_stretch`, which 56 pipelines depend on. `core` keeps the keys and reproduces all 18 recorded cases. Fifteen unreferenced registrations went to `removed.json` |
| P3-T07 | Switch VXL off and delete its sources | P3-T06 | done | 99ec1955b, 244755658 | VXL is off in kwiver, fletch and VIAME, and the `vxl_*` names are aliases of the in-house implementations. All 84 golden cases pass with VXL absent, including all 9 whole-pipeline recordings bit for bit. `kw_archive_writer` is removed on the user's decision on open decision 3; `filter_to_kwa.pipe` existed only to write one and is deleted, and five indexing and query pipelines dropped the side output, which is the whole of the deliberate `pipes.json` regeneration (the other 34 files differ only in exception message text, which embeds absolute source paths and which `compare_pipes.py` does not compare). `vidl_ffmpeg` is a second registration of kwiver's `ffmpeg` reader, as `lite-removals.md` section 3 says it should be; the six config keys the two do not share are in the new `pending.json`, the mechanism `phase-01` defines for a contract knowingly relaxed between phases, and section 3.1 folds them into the phase 4 PyAV reader. The flip exposed two defects, both fixed: the new algorithms had `PLUGGABLE_VARIABLES` and friends spelled out separately, copied from the `plugins/vxl` header that had the ODR clash, and that spelling omits `get_configuration`, so a partial config from a pipe file threw on the first unset key; and the pipeline golden runner inherited an environment that importing the kwiver python package had rewritten, adding the reference install to `LD_LIBRARY_PATH`, so it now sources the install itself |
| P3-T12 | `vidl_ffmpeg` video_input bridge | P3-T02 | dropped | | Added by P3-T07, then dropped: writing a C++ wrapper was inventing an implementation the plan does not have and phase 4 would delete. `lite-removals.md` section 3 already treats `vidl_ffmpeg` as an older name for the `ffmpeg` reader and section 3.1 folds its keys into the phase 4 PyAV reader, so P3 registers the alias and `pending.json` carries the gap. The recording of what the VXL reader did with each key, taken while it still existed, is committed as `tests/golden/video/manifest.json` for P4-T01 |
| P3-T08 | Migrate pipeline files to new names | P3-T07 | done | 8642ff6bc | `design/scripts/rename_impls.py`, 407 substitutions over 79 files in `configs/` and `examples/`; add-ons are left alone and keep resolving through the aliases, which is what the aliases are for. Every pipeline keeps its status; the only baseline changes are the renamed implementations and the config key paths that embed an implementation name. `pending.json` is not deleted as the task text says: it holds the `vidl_ffmpeg` keys phase 4 restores rather than the VXL names phase 1 would have put there |
| P3-T09 | (conditional) `kw_archive_writer` without VXL | P3-T07; only if open decision 3 = keep | skipped (decision 3 = drop) | | The user settled open decision 3: the KWA format is no longer needed |
| P3-T10 | Build kwiver locally so `VIAME_ENABLE_VXL` reaches it | P3-T02 | done | 0e97eb1a0 | Added by P3-T03; blocks P3-T06 and P3-T07. Only needed because phases 1 and 2 are deferred and this tree had been reusing the reference superbuild's kwiver; the flag itself was already wired. Built from the submodule pin, 64d8306. Building instead from the neighbouring checkout's working tree, which is 4 commits ahead, moved 13 `perform_query` and `handle_descriptor_request` entries and failed `baseline:registry` -- the check doing its job. The lite tree builds what its own submodule pins, and BASELINE and GOLDEN pass unchanged against the locally built kwiver |
| P3-T11 | `close_loops_homography_guided` and its polygon overlap | P3-T02 | done | 3f5201a2a | Added by P3-T10's usage scan: `lite-removals.md` §1 misses this name, which `common_image_stabilizer.pipe` really does select. Registered as `homography_guided`. Its only VXL use was `compute_homography_overlap`, reimplemented as convex polygon clipping and A/B'd against the VXL routine over 52 homographies to within 1e-9 |
| **Phase 4: drop FFmpeg** | `phase-04-drop-ffmpeg.md` | | | | |
| P4-T01 | Record video golden data and benchmark baseline | P3-T08 | done | 8122fea83 | `pipelines_test_data` ships no video at all, so three clips were generated from the committed image fixtures and committed: 8 bit h264, 10 bit h264 and variable frame rate. The 10 bit one decodes to `uint16`, which is the behaviour `lite-removals.md` section 3.1 asks the PyAV reader to keep, and the VFR one has genuinely uneven frame gaps, so a reader that derives timestamps from the frame number fails it. Recorded from the C++ `ffmpeg` reader, which is what phase 4 replaces. The 1080p throughput clip is built at record time rather than committed |
| P4-T02 | PyAV `video_input` | P4-T01 | done | df125cce8, 5281cfcfa | Functionally exact: frame counts, numbering, dtypes and pixel digests match the recording on all three clips, and timestamps match on the constant rate ones. Two things had to be matched rather than assumed. The conversion goes through a libavfilter graph carrying the swscale flags from `arrows/ffmpeg/ffmpeg_convert_image.cxx` (`full_chroma_int+full_chroma_inp+accurate_rnd+bitexact+neighbor`, `out_range=full`); PyAV's `to_ndarray` defaults differ from the C++ reader by up to 3 counts on nearly every pixel. Timestamps are relative to the first frame and rounded to whole microseconds, taken from the frame's own time base because filters rescale it. Two residual differences: variable frame rate timestamps can differ by one container tick, because the C++ reader used libavcodec's `best_effort_timestamp` and PyAV does not expose it, and throughput, which was 46 per cent of the C++ reader until the cause was found: vital stores images planar, so handing `Image` an interleaved numpy array drops out of `copy_from`'s memcpy path onto a per pixel walk, 14 ms a frame at 1080p against 0.5 for a memcpy. The filter chain now emits planar RGB and the planes are assembled in vital's own layout, giving 82.4 fps against 99.1, or 83 per cent, past the task's 60 per cent gate |
| P4-T03 | PyAV `video_output` | P4-T02 | done | 1b28b1118 | Encoder output is identical: writing the same 30 frames through the C++ arrow and through this one gives 29 of 30 byte identical packets and decoded pixels that match exactly, the odd packet being the parameter sets, which the arrow wrote in band and PyAV puts in the container's global header. Three things had to be matched rather than assumed. `lite-removals.md` section 3.2 names the config keys `codec`, `bit_rate` and `frame_rate`; the registered ones are `codec_name`, `bitrate` and `frame_rate_num`/`_den`, and those are what `registry.json` holds a replacement to, so the registered spellings are kept and section 3.2 is the summary that drifted. The same section gives the default codec as h264, where the arrow's priority order picks H.265 first for every container VIAME writes, so this writer does too. And PyAV gives a new stream a default bit rate, which puts x264 and x265 into average bit rate mode where the arrow got their constant quality default, so the field is set from the config unconditionally, zero included. Along the way: the arrow loses the last frame of every video it writes. It muxed packets with no duration, so the mp4 muxer derived each sample's duration from the next sample's decode time and gave the final one zero; the track then ends before its last sample and every decoder trims it. `filter_to_video.pipe` over six frames writes five. Recorded as the golden with the fix spelled out per field in `tests/golden/test_video.py`, since the recording is of the defect. The writer is registered as `pyav`; it takes the `ffmpeg` name in P4-T05, which also has to fix `video_output_process` (see the note added to that task). P4-T02 was to add `av` to the requirements and did not: the wheel was reaching the install only as a transitive requirement of `pytorchvideo`, so a CPU build would have had no video at all. Declared in `add_project_python_deps.cmake` until phase 1 makes the lock files |
| P4-T04 | `ffmpeg_cli` fallback reader | P4-T02 | done | e45c5485a | Exact, and on the one clip the PyAV reader could not match exactly it is closer: the CLI reader reproduces all three recorded clips' frame counts, pixel digests and timestamps to the microsecond, `clip_vfr.mp4` included, because `showinfo` reports the presentation time after the same heuristic the C++ reader used, which PyAV does not expose. Seeking is exact too, so the precision the task allowed to be documented as reduced is not: `-ss` before `-i` with `-copyts` decodes to the frame asked for and keeps the original timestamps. Three things had to be got right. The raw output path defaults to a constant frame rate and duplicates frames to fill a variable rate source, turning the 30 frames of `clip_vfr.mp4` into 34, so `-fps_mode passthrough` (FFmpeg 5.0 or newer) is set. Pixels come out as `gbrp` rather than the `rgb24` section 3.3 asks for, because vital stores images planar and the interleaved layout costs a per pixel walk. And presentation times arrive on stderr while frames arrive on stdout, so stderr is read on its own thread; leaving it unread fills its buffer and stalls the decode. `use_cli` on the PyAV reader forces this path, and it is taken automatically when `import av` fails, which is checked in a child process with `av` blocked from the import machinery rather than by uninstalling it. Also fixed in both readers while writing the tests: seeking before any frame had been read took the timestamp origin from the frame seeked to, so every time afterwards was measured from there rather than from the start of the video. `imageio-ffmpeg` is declared alongside `av` |
| P4-T05 | Switch FFmpeg off | P4-T03, P4-T04 | done | 77c87fe2d | FFmpeg is off in kwiver and fletch, the `VIAME_ENABLE_FFMPEG*` options are gone, and nothing kwiver or VIAME installs links `libav*`; the only libraries that still do are FFmpeg's own and VXL's `libvidl`, both left in the install by the reference build this one is seeded from. `ffmpeg` and `vidl_ffmpeg` are the PyAV reader and `ffmpeg` the PyAV writer, each as a trivial subclass, because a python implementation is discovered by class and so an alias cannot be a second registration of one class the way the C++ ones are. The keys took the most care. The PyAV reader now carries all twelve of the C++ reader's and all six of vidl's with their recorded defaults, which empties `pending.json`; `approximate` and `time_source` are honoured rather than merely accepted, the latter for `start_at_0`, `current` and `none`, which is every value any shipped config uses. Worth knowing for later phases: `registry-dump` cannot introspect a python implementation's config, so `compare_registry.py` silently stops checking the keys of any name that becomes python. The contract for these names is held by a unit test against `registry.json` instead, and every later phase that moves a C++ implementation to python needs the same. `video_input/ffmpeg_clip` and `image_io/ffmpeg` had no users and go to `removed.json`. `video_output_process` was passing a null `video_settings` whenever `WITH_FFMPEG` was undefined, which would have left the writer with no geometry or frame rate; it uses `vital::simple_video_settings` now and that directory no longer links FFmpeg at all. `tools/add_segmentations.py` was shelling out to the ffmpeg binary fletch installed, and now finds the `imageio-ffmpeg` one. The DIVE smoke test the task asks for needs the GUI and was not run; the transcode pipelines, which are what DIVE's video export drives, pass, as do `filter_to_video` and the CRITICAL examples |
| **Phase 5: import the kwiver subset** | `phase-05-import-kwiver.md` | | | | |
| P5-T01 | Reachability list | P4-T05 | done | 886ba83e2 | 902 files, 157857 lines, in `design/lite-kwiver-files.txt`; the task expected roughly 120k to 150k. The script scans `plugins/` as well as `library/` and `tools/`, because phases 1 and 2 are deferred and most of VIAME is still there. Every directory the sanity list says must be absent was reached, and none of them by an include: each was reached only through its own registration file, because `registry.json` records names those arrows register. `apply_child_klv` and `update_klv` (arrows/klv), `derive_corner_points` (arrows/geocalc), `cuda` (arrows/cuda), `template` (sprokit's worked example) and `file_transport_send` (sprokit/processes/transport) have no user anywhere in the shipped pipelines, configs or VIAME's own code, and each was the single reason its directory was reached, so they go to `removed.json` the way the unused vxl registrations did. `arrows/serialize` was reached by a false match: the applet named `json` matched `arrows/serialize/json/algo/register_algorithms.cxx`, so names are now looked up only in the registration file for their own kind. Two things the closure cannot see. Processes are registered by type rather than by a quoted name, so `sprokit/processes/core` was missed until the script also matched `<name>_process`; without that fix the list had no processes in it at all, which is most of what VIAME runs. And kwiversys is absent because its headers are `.h.in` templates configured into the build directory, so nothing in the source tree resolves an include of them; it is used, and the whole directory moves to `third_party/` as the layout says. `arrows/klv`, `arrows/geocalc`, `arrows/cuda` and `sprokit/processes/transport` are still built here; turning them off belongs with the copy in P5-T02 and after |
| P5-T02 | Copy `vital` into `core_types` and `algorithm_framework` | P5-T01 | done | 080b61004, fbbb40937 | Landed on the second attempt, with kwiver's vital deleted rather than duplicated. The first attempt followed the task text -- copy vital, point VIAME at the copy, leave kwiver building its own -- and every `kwiver runner` process then aborted in `__run_exit_handlers` with a double free, eight times out of eight; taking the copied library out of the install stopped it, three times out of three. Identical code and identical guards are why it built and ran at all, but the static state is not shared and two sets of it do not survive teardown. So the whole of kwiver moved onto the copy in one step: 957 sources had their includes rewritten from `vital/` to `viame/`, 442 files were deleted from `packages/kwiver/vital`, and the ten vital library targets keep their names and their split but are built from `library/core_types` and `library/algorithm_framework`. Each of their CMakeLists rebases kwiver's own file list onto the imported tree and reports at configure time which files did not come across, so the difference from upstream is visible rather than silent. Five things the closure could not see, each found by a compile or a link error. cereal is vendored whole rather than file by file, with the rapidjson it ships left inside it; splitting it as the plan had it would mean patching a vendored library. Twenty-three more files came across, among them the two algorithm interfaces only VIAME's python implements -- nothing includes their headers, so no closure over includes can reach them, and the script now carries them explicitly. The `vital.algo` bindings are generated by castxml from those headers, so the generator's input directory moves with them while the include paths it writes do not, which used to be the same string. And the tree this build takes fletch from also holds an older kwiver's headers, which now shadow the imported ones, so that prefix is off the include path and the two plugins that needed a header from it name it themselves. `arrows/klv`, `arrows/geocalc` and `arrows/cuda` are off, P5-T01 having removed the only registrations they had; the klv applet goes with them. The python bindings stay in kwiver until P5-T05, because two `kwiver.vital.types` extension modules in one interpreter is a pybind11 duplicate-registration error. `library/core_types` is 27684 lines and `library/algorithm_framework` 30480, before P5-T06 prunes |
| P5-T03 | Copy sprokit into `pipeline_framework` | P5-T02 | done | 488f2eeac | 119 files: the pipeline engine, the `.pipe` parser and bakery, the schedulers, the runner, `pipe-config` and `pipe-to-dot` applets, the input and output adapters, the type traits, and `downsample_process`. Kwiver's copies deleted in the same change, as vital's were, and 336 sources had their includes rewritten. Whole directories this time rather than the reachability subset: these are complete libraries that kwiver builds as units, and registration by type is invisible to a closure over includes, so taking the subset would have lost most of it. `sprokit/processes/core` is the one place a subset is taken, and only `downsample_process`; the rest goes to the functional libraries in P5-T04. `pipeline` and `pipeline_util` flatten into one directory, sharing no file name, and `kwiver_type_traits.h` is `type_traits.h` there. Two things vital's move did not need: the adapters and the generic processes include `kwiver_type_traits.h` by bare name, reached through a directory on kwiver's include path rather than a path of its own, so the rewriter now carries the bare names that moved; and four more generated export headers needed republishing under the viame prefix. `sprokit/processes/transport` is not built, P5-T01 having removed the only one of its processes this build registered. 28159 lines |
| P5-T04 | Copy used arrows and processes into functional dirs | P5-T03 | done | ac0aa151b, and this commit | Landed as six commits, one per destination, each built and tested before the next. `arrows/core`, `arrows/ocv`, `arrows/mvg` and `sprokit/processes/core` are all gone, along with the two arrow applet directories and `sprokit/processes/examples/process_template`. Four things the task text did not anticipate. The arrows/core estimate of "5 used registrations" was low: `common_image_stabilizer.pipe` is shipped, two other pipelines include it, and it pulls in the whole feature tracking chain -- `track_features:core`, `homography_guided` matching, `filtered` detection, `nonmax` filtering and two loop closers -- so twelve came across rather than five, and all of them to `image_processing` alongside the ocv SIFT and SURF the same pipe selects. `arrows/ocv`'s cv::Mat bridge had no row in the layout table because the finished tree has no such code; sixty-four files include it, it could not stay in kwiver and it must not go in `image_ops`, so it is `library/opencv_bridge`, marked transitional and deleted by P7 (finding 1.12). Anything left in kwiver that then needed a VIAME library had to move with it, which is why the process template is in `library/examples` now (finding 1.11). And VIAME's own build never set `KWIVER_OPENCV_VERSION_MAJOR`, which the imported bridge branches on, so the top-level CMakeLists sets it the way kwiver did. Nine removals recorded beyond the arrow registrations: the two mesh algorithms and the five kwiver applets, each dead for a reason the entry gives. 25 of 25 tests pass |
| P5-T05 | Remove the kwiver submodule | P5-T04 | done | this commit | `packages/kwiver` is gone. It took nine commits, each built and tested. The python package to `python/kwiver`, the arrows and the unbuilt process directories deleted, kwiversys to `third_party/` (where an unused byte-identical copy had sat since P5-T02, which is what open question 2.3's "two copies" was), vital's plugin and test-interface directories to `library/algorithm_framework`, sprokit's example processes and `thread_pool` scheduler to `library/`, kwiver's macro library to `cmake/kwiver_compat`, the tool runner to `tools/`, and finally the CMake itself: `library/pipeline_framework` builds sprokit, `library/algorithm_framework` and `library/core_types` build vital, `python/` builds the bindings and `cmake/kwiver_compat/kwiver_settings.cmake` holds what kwiver's top-level did for them. `viame-config.cmake` in `lib/cmake/viame` replaces kwiver's config package, keeping the `kwiver::` namespace so an out-of-tree plugin that linked `kwiver::vital` still does; `examples/plugin_creation` builds against it, which meant porting it to the current algorithm API -- it had been written against `algorithm_impl<>` and `algorithm_factory.h`, which kwiver 2 replaced, so it had not compiled in a long time. Four things only the move revealed: `viame_lite_rebase` had been dropping forty-eight files silently, which the literal lists now do not; python extension modules cannot be linked with `-Wl,--no-undefined`, which VIAME sets and kwiver did not; `viame_image_ops` had to join the export set once VIAME had one of its own; and `kwiver-install-utils` reads its destination out of a variable named for kwiver. The superbuild's kwiver ExternalProject is deleted and VIVIA and SEAL depend on `viame` instead -- that path is not exercised here and phase 1 rewrites it. 25 of 25 tests pass |
| P5-T06 | Prune unused `vital` code by compile | P5-T05 | done | this commit | `library/core_types` is 26477 lines in 136 files (was 27684 in 147) and `library/algorithm_framework` 25665 in 221 (was 31172 in 285); `library/pipeline_framework` is untouched at 28461. Three passes. Twenty-one `algo` interfaces with no implementation left anywhere -- their implementations went with the vxl, ffmpeg, mvg and ocv removals of phases 3 to 5 -- and their python bindings, which drop themselves when the header is gone. Then the headers a closure over `#include` from everything outside the two directories never reaches: six core types (ground control points, mesh and tiled-image containers, the metadata streams) and eleven framework files (the thread pool and its three backends, hex_dump, simple_stats, applet_config, config_validation, the camera-map, landmark-map and track-set IO, config_camera_helpers, pluggable_macro_testing and test_interface.h). And the CMake for dependencies that are gone: the twenty-three `kwiver-depends-*` files, kwiver's setup scripts and dashboard scripts, its config package templates, and the FFmpeg, log4cxx and PROJ finders, which came off the list `viame-config.cmake` puts on a consumer's module path. One thing the closure gets wrong in both directions and has to be checked by hand: a header used only by a compiled sibling `.cxx` looks unreached (`class_map.txx`, `math_constants.h`), and a bare `#include "x.h"` resolved through `-I` is invisible unless the resolver guesses by basename (`camera_io.h`, which three VIAME plugins include that way). Of finding 2.5's eleven over-kept `ocv` and `core` implementations only one turned out to be safely removable, `filter_tracks:core`; the rest are nested algorithms of something kept, or the only implementation their interface has. 25 of 25 tests pass |
| P5-T07 | DIVE smoke and docs | P5-T06 | done | this commit | Smoke run headlessly rather than through DIVE's GUI, which cannot be driven from here: each of P1-T10's four actions was run the way DIVE runs it, against the installed prefix with `setup_viame.sh` sourced. Open an image folder and detect -- `kwiver runner detector_default_fish_no_motion.pipe` over the nine-frame fish sequence, 31 detections written. Track -- `tracker_generic_proposals.pipe`, 11 track rows. Train -- `viame train -c train_detector_netharn_cfrnn.conf`, 16 epochs in seven minutes with the loss falling, stopped at the timeout the way the CRITICAL test does. Score -- `viame score` against the ground truth CSV, precision 0.35 recall 1.00 AP@50 1.00 over 9 frames. The applet list `viame help` prints matches the baseline. Two things the smoke found rather than the test suite: `lib/kwiver/plugins` still held five plugin `.so` files from before P5-T05, one of which made the loader warn about a duplicate `config_explorer` factory on every invocation (finding 1.9 again); and `docs/manual` had an orphaned `core_types.rst` including a path inside the deleted submodule. The manual's toctree loses its link to KWIVER's arrows, which no longer exist, and says why the other two stay |
| **Phase 6: drop Eigen** | `phase-06-drop-eigen.md` | | | | |
| P6-T01 | Inventory Eigen usage | P5-T07 | done | this commit | `design/lite-eigen-uses.txt`. 243 lines under `library/` and `tools/` name Eigen, in 41 files: 229 are a type alias or a declaration, nine are a decomposition, four are a map, block or cast idiom, one is arithmetic. That undercounts the port, because vital's math typedefs are Eigen types under other names -- `vector_3d`, `matrix_3x3d`, `rotation_d`, `covariance_2d` -- and a line using one depends on Eigen's operators without saying so. Counting those, 96 files across `library/`, `plugins/` and `tools/` are in scope and 847 lines name a typedef; `plugins/pytorch/learn` is excluded, since the python packages vendored there carry their own Eigen and become wheels in phase 9. The member functions those files call on a math type are the specification for `core_types/math`, and there are fourteen of them: dot, transpose, inverse, setZero, setIdentity, normalized, normalize, norm, squaredNorm, determinant, cross, head, block, hnormalized -- plus the operators and the four decompositions. Nothing reaches for a sparse type, a solver, a geometry module beyond Quaternion, AngleAxis and AlignedBox, or an expression template that is not a block or a cast |
| P6-T02 | `core_types/math` library | P6-T01 | done | this commit | `library/core_types/math`: `vector.h`, `matrix.h` with `matrix_detail.h` for the closed-form determinant and inverse at two, three and four, `dynamic_matrix.h`, `decomp.h` and `quaternion.h`. Header-only, column-major like Eigen so that `data()` hands the same bytes to numpy and OpenCV, and no expression templates: the largest vector VIAME has is four elements, so the copies are four doubles and the aliasing questions an expression template exists to answer do not arise. `decomp.h` is one-sided Jacobi for the SVD, cyclic Jacobi for symmetric eigenvectors, plus Cholesky and a least-squares solve through the SVD -- nine lines of VIAME wanted a decomposition and those four answer all of them. `tests/golden/math/eigen.json` holds 380 cases recorded from Eigen by `record_from_eigen.cxx`, which is deliberately not built: Eigen is the thing being removed, so a target needing it would have to be deleted in the same change that makes the recording worth having. `ctest -R viame:math` is seven tests and passes; the recording says the two agree and the identities checked alongside it -- `A*inverse(A) == I`, `U diag(s) V^T == A`, `A v == lambda v`, `L L^T == A`, orthogonality and unit determinant of a rotation -- say the answer is right. Worst absolute disagreement with Eigen across the whole set, measured before the recording was frozen: 2.3e-13 for the matrix work, 2.6e-14 for the quaternions |
| P6-T03 | Port `core_types` off Eigen | P6-T02 | done | this commit | `git grep Eigen:: -- library` is empty. Two headers had to be written that P6-T02 did not anticipate: `math/aligned_box.h`, because `bounding_box` is an `Eigen::AlignedBox` under another name, and `math/dynamic_vector.h`, because `camera_intrinsics::dist_coeffs` is a `VectorXd`. `core_types/casters.h` replaces `pybind11/eigen.h` in 36 binding sources -- a `vector_< N, T >` casts to a 1-D numpy array of N and a `matrix_< R, C, T >` to a 2-D one, and a load accepts (N,), (N,1) or (1,N) the way pybind11's Eigen caster did. What did not survive mechanically was the expression idioms: `.array()` became `cwisePlus`/`cwiseMin`/`cwiseMax`, an assignable block (`v.head( 2 ) = ...`, `m.row( i ) = ...`) became a named element or a `set_row`, `computeInverseWithCheck` became a determinant test and an `inverse()`, and `ldlt().solve()` and `colPivHouseholderQr().solve()` both became `solve()`, since every one of them is square and the factorisation was never the point. `library/core_types/{vector,matrix}.h` stay as two-line shims so that the 847 lines naming a typedef did not have to move |
| P6-T04 | Port `algorithm_framework`, `pipeline_framework`, `utilities`, `file_io` | P6-T03 | done | this commit | Fell out of P6-T03 rather than being a task of its own: these directories reach Eigen only through headers `core_types` publishes, so porting `core_types` ported them. The one file with work of its own was `algorithm_framework/io/eigen_io.h`, which keeps its name and moved from `namespace Eigen` to `namespace kwiver::vital` -- it holds the throwing `operator>>` and the cereal `serialize` for the math types, and the plain `operator>>` came out of `math/vector.h` and `math/matrix.h` so that there is one of each. `config_block`'s Eigen conversions the task text expects do not exist in this tree |
| P6-T05 | Port `measurement` and `object_trackers` | P6-T04 | done | this commit | `library/measurement/triangulate.cxx` is the only real work: two `Eigen::JacobiSVD` over dynamic matrices became `jacobi_svd`, the `Array3d` arithmetic became plain vector arithmetic with `cwiseMin`/`cwiseMax`, and `M.colPivHouseholderQr().solve( v )` became `M.solve( v )` (M is 3x3 and full rank whenever there are two non-parallel rays). **The task text's python move was not needed.** It asks for `calibrate_cameras_from_tracks_process` and `optimize_stereo_cameras` to move their numeric optimisation into python on scipy `least_squares`; nothing in this tree uses Ceres, and both of those reach Eigen only for type spellings -- the numerics are OpenCV's `stereoCalibrate` and `calibrateCamera`. Moving them would have been a rewrite phase 6 has no reason to ask for. If they should move it is for a different reason, and belongs in phase 7 or later. **This row shipped a defect, and it is the largest of the project so far.** `Triangulate_DLT` assigns its four rows with `design_matrix.row( n ) = ...` and `camera_perspective` builds its 3x4 matrices with `P.block< 3, 3 >( 0, 0 ) = R`. Eigen returns writable proxies from `row()` and `block()`; `core_types/math` returns **values**, and assigning to a value compiles -- it copy-assigns to a temporary which is then destroyed. So `pose_matrix()` returned a zero matrix and the DLT design matrix was zero, and **every stereo measurement VIAME computed on this branch came out zero**, against 361.14 mm on the reference build. `similarity_::matrix()` was a third, setting its corner to 1 and leaving the rotation and translation blank. Fixed under P7-T06, where the measurement golden that would have caught it finally exists. The lasting change is the guard rather than the ten call sites: `matrix_` and `vector_` now ref-qualify their assignment operators, so assigning to a temporary is a compile error -- which found `similarity.cxx` by itself on the first rebuild. Finding 1.20 |
| P6-T06 | Port the rest and delete Eigen | P6-T05 | done | this commit | Eigen is gone from VIAME's build: no `find_package( Eigen3 )`, no `Eigen3::Eigen`, none in the installed config package, and `fletch_ENABLE_Eigen` is OFF -- the only other thing in the enabled fletch set that wanted it was OpenCV's optional `WITH_EIGEN` interop. There is no `third_party/eigen` to delete; Eigen came from fletch. Two things the task text did not foresee. First, the remaining users are in `plugins/`, not in the `library/` names the text uses: those are phase 2's layout and phase 2 is deferred, so P6-T04 to P6-T06 read against a tree that does not exist yet -- nine files under `plugins/core` and `plugins/opencv` were the actual work. Second, **`opencv2/core/eigen.hpp` had to go in the same change.** It is guarded on `EIGEN_WORLD_VERSION` and `#error`s when Eigen's headers have not been included first, so it cannot outlive Eigen even by one commit. `library/opencv_bridge/matrix.h` replaces its `cv2eigen` and `eigen2cv` with `mat_to_matrix`, `matrix_to_mat`, `mat_to_vector` and `vector_to_mat`, which throw on a shape mismatch where OpenCV's asserted. That is P7-T04's territory brought forward |
| **Phase 7: drop OpenCV from the C++ build** | `phase-07-drop-opencv.md` | | | | |
| P7-T01 | Inventory and golden recording | P5-T07 | done | this commit | `design/lite-opencv-uses.txt`: 96 files include `opencv2/`, 52 of them under `plugins/opencv` alone -- `plugins/` is in scope because phase 2 has not moved those directories yet, so most of what phase 7's task text describes still lives there. Per file, the section 2 areas and the `cv::` names; then two tables the per-file list cannot give you, the 36 registered algorithm names OpenCV backs and the 23 processes, each resolved back to the class that defines it. Resolved by preprocessing every `register_algorithms.cxx` with its own target's include paths, because most of them register through a macro and reading the source unexpanded gets it wrong; cross-checked against `viame registry-dump`. Three recordings, 63 cases in all. `codecs` is 18 containers through the `ocv` image_io -- PNG 8 and 16 bit gray, RGB and RGBA, JPEG at 4:2:0 and 4:4:4, BMP, TIFF stripped at 8 and 16 bit uncompressed, LZW and PackBits, and one tiled TIFF -- plus a write-then-read round trip per extension, which is what records BMP losing 16 bit gray on the way out. `calib` is `viame::read_stereo_rig` on five committed calibration sources and `cv::FileStorage` on five documents, as JSON, compared exactly. `opencv` is 16 image_filter cases, four split_image, two detect_motion, four detector cases recorded as detections rather than pixels, and seven whole pipelines; its own three fixtures (circles, a heat map, a BG Bayer mosaic) exist because the VXL fixtures are gradients and bars, which say nothing about a detector. `design/scripts/{expand_registry.sh,ocv_backed.py,ocv_procs.py,opencv_inventory.py}` regenerate the inventory, so it can be re-derived rather than trusted. Two things had to be fixed to record any of it. `test_golden.py` skipped by implementation name alone, so a `vxl` bundle_adjust removed in P5-T04 had silently turned off the twelve `vxl` image_io cases since phase 3 -- see finding 1.16. And `pipeline_runner` treated an unchanged input as the only kind of input, so the six shipped filter pipelines that connect `input.file_name` to the writer and overwrite their input in place looked like pipelines that wrote nothing. The recordings also pin what each implementation **refuses**, which no output comparison could catch. One refusal turned out not to be one: `ocv_random_hue_shift` throws on a single channel image only when its trigger draw fires, about half the time -- finding 1.10 |
| P7-T02 | Codecs | P7-T01 | done | this commit | `third_party/stb` (stb_image v2.30, stb_image_write v1.16, pinned to one commit with digests) plus `library/video_io/codecs`: `image_codec.{h,cxx}` as the front door and `tiff.{h,cxx}`, which is in house because stb has never done TIFF and VIAME's data is full of it. The TIFF reader covers the baseline subset -- strips, contiguous samples, 8 and 16 bit, 1, 3 and 4 samples, uncompressed, LZW and PackBits, with the horizontal differencing predictor -- and writes uncompressed strips. `core_image_io` decodes through it and is registered as `core` with `vxl` and now `ocv` aliased to it; `arrows/ocv`'s `image_io` is deleted. Anything the codecs decline goes to `pil`, a new python image_io on Pillow: a tiled TIFF, a 32 bit one, a WebP. The recording is what made this checkable and what caught three things a self-consistent implementation would have got wrong. OpenCV **saturates** when narrowing 16 bit to 8 for a container that cannot hold it -- not a shift, not a rescale. A gray BMP is palettised 8 bit, and stb writes 24 bit BGR, so a gray image round tripped through stb comes back three planes; `encode_gray_bmp` writes the palettised one. And stb expands any palette to RGB on read, so a gray BMP decodes to three planes unless the palette is inspected first. All twenty containers now decode identically to OpenCV -- exactly for every lossless one, max 3 counts for JPEG against a tolerance of 8 -- and the write-then-read round trips agree for PNG, BMP and TIFF. 13 unit tests beside them, checking the formats rather than the agreement. Two fixture kinds were added while doing it: a predictor-2 LZW TIFF at 8 and 16 bit, since Pillow always writes predictor 1 and the predictor is the one TIFF feature a reader can get subtly wrong; and 4:4:4 JPEG, since 4:2:0 chroma on a synthetic image moves a pixel by over a hundred counts and would hide a decoder difference underneath it. **A defect found on the way:** every VIAME python module was being installed one directory above site-packages, where nothing imports it, and worked only because an older install had left a copy in the right place -- finding 1.17 |
| P7-T03 | `image_ops` v2 | P7-T01 | done | this commit | `library/image_ops/color.h` first, because `cvtColor` is the most-asked-for OpenCV call in the tree -- twelve files. RGB to and from gray, HSV and L*a*b*, the RB swap, and a bilinear Bayer demosaic for all four patterns. The arithmetic is OpenCV's rather than a textbook's, because the outputs are held to a recording of what OpenCV produced: BT.601 at 14 fixed-point bits for luminance, hue in halved degrees, L scaled to 0..255 and a and b offset by 128. `pixel.h` gains `saturate_pixel`, since a colour round trip leaves the gamut and wrapping there is a black pixel where a white one belongs. `tests/golden/image_ops/opencv.json` is the counterpart of the Eigen recording: run `record_from_opencv.py` once while OpenCV is still there, commit the result, and the C++ test outlives it. Eight cases over a 32 by 24 window, 122 KB. Measured against it: demosaic **exact** away from the window's edge, HSV, L*a*b* and both round trips within 1 count, luminance within 1 on ten pixels of 768 where the value lands on a half. The plan asks for demosaic within 3; the tolerances in the recording are 1 and 2, one above what was observed. `tests/library/golden_json.h` is the thirty-line JSON reader, shared now rather than copied from the math test. Then `filter.h`: `filter2D`, Gaussian and box blur, Sobel at orders 1 and 2, `addWeighted`, and the four `BORDER_` rules. Sixteen more recorded cases, and **every one is exact** but two that differ by a single count -- an explicitly-given sigma, where OpenCV works in float and this in double, and the compounded `addWeighted`. Two things that would have been silently wrong without the recording. `cv::getGaussianKernel` does **not** derive a sigma from the size for a small kernel: at seven or less it returns a fixed table, and (0.25, 0.5, 0.25) is a binomial rather than a sampled Gaussian, so deriving instead gives a visibly different blur at size three -- the size the pipelines use most. And `getDerivKernels` is not "a binomial, differenced": it is a binomial of length `size - order` convolved with `order` copies of (-1, 1), each of which *lengthens* the row. Differencing the length-`size` binomial gives (2, 0, -2) where OpenCV gives (-1, 0, 1) -- wrong sign convention and wrong scale. Then `warp.h`: resize in three modes, perspective and affine warps, `getRotationMatrix2D` and `remap`. Fifteen more cases; every resize mode exact or within 1. Two more OpenCV conventions that a sensible implementation gets wrong. `cv::resize` with `INTER_NEAREST` **floors** an uncentred grid while `cv::warpPerspective` with the same flag **rounds** -- one word, two conventions, and using the centred grid for a nearest resize disagrees on a third of the pixels of an enlargement. And `INTER_AREA` when enlarging is the area rule, not, as OpenCV's own documentation says, "similar to INTER_NEAREST": treating it as nearest is eighty counts out. The bilinear warps agree to four counts rather than one, and the difference is OpenCV's: it interpolates a warp in fixed point at INTER_BITS 5, quantising the position to a thirty-second of a pixel. Rather than reproduce a quantisation that loses accuracy, the tolerance says 5 and a test measures both against the exact bilinear answer -- `image_ops` 0.254 mean error, OpenCV 0.366 -- so "we differ because we are more accurate" is checked rather than asserted. Then `histogram.h`: `calcHist`, `minMaxLoc`, `normalize`, `equalizeHist` and CLAHE, which is what thirteen shipped pipelines select `ocv_enhancer` for. Nine more cases. `equalizeHist` is **exact**, including on a large flat background where OpenCV's mapping and the textbook one differ across the whole range -- it divides by `total - count_of_the_first_occupied_bin` so the darkest occupied value maps to zero. CLAHE is exact on a flat image at any size and within 1 count on a real one. Two more things pinned by recording rather than reasoning. `cv::saturate_cast` rounds **half to even**, and a cumulative histogram scaled to a byte lands on an exact half constantly, so `pixel.h` gains `saturate_pixel_even` beside the away-from-zero one. And CLAHE's tile padding: OpenCV pads when *either* dimension fails to divide by the tile grid and then pads *both* by `tiles - (extent % tiles)`, so a dimension that already divides gains a whole extra tile -- a 16 by 12 image on an 8 by 8 grid becomes 24 by 16 and the tiles are 3 by 2. Rounding up instead, which is the obvious thing, is wrong on any size that does not divide evenly, which real frames rarely do. Then `contours.h`: connected components at four and eight connectivity, outer contour tracing, contour area, bounding rect, convex hull and minimum-area rect. Recorded against a mask with an L, a ring, a diagonal bar of paired pixels, a single pixel and two squares touching only at a corner -- shapes whose box, area, hull and rotated rect all differ, and one that eight-connectivity joins and four does not (ten components against five). Component labelling is compared as a partition rather than by label value, and every contour measurement matches OpenCV exactly. The trace goes through the components rather than starting wherever a foreground pixel has background to its west. That textual Suzuki and Abe condition for an outer border also fires on the *inside* of a ring, so a ring came out as two contours; distinguishing them needs the nesting level Suzuki and Abe carry, and one outer boundary per component is what `RETR_EXTERNAL` means anyway. Then `match.h`, `layout.h`, `draw.h` and `font_5x7.h`: template matching, concatenation, and rectangles, lines, circles, filled polygons and text. Ten more cases and **every drawing primitive is byte-identical to OpenCV**. Three more conventions caught by recording. `cv::matchTemplate` subtracts a **per-channel** mean, not one over the whole patch -- on a colour image the two differ by up to a whole unit of correlation, and on a grey one they coincide, so only the colour case says so. `cv::circle`'s outline is its own rasterisation, not the midpoint circle: forty of the two hundred pixels of a radius nine ring differ. And `cv::fillPoly` draws the **outline as well as** the interior, which "fill" does not suggest -- twenty nine pixels of a difference on a four-sided polygon. Text is the one thing that cannot be compared: `font_5x7.h` is a bitmap font and OpenCV's is Hershey's, so the glyphs differ by construction. The tests check what a font has to be true of instead -- draws inside the cell it reports, advances by the width it reports, every printable character has a glyph, a space has none. **This task is complete apart from** `image_ops` having no caller yet: P7-T04 onward is what replaces the OpenCV implementations with these. `library/image_ops` has no OpenCV include and never has |
| P7-T04 | Port `image_processing` and `object_detectors` C++ | P7-T02, P7-T03 | done | this commit | Five ported so far, all against the recording: `split_image` and `ocv_channels` (exact), `merge_images`, `ocv_write` and `draw_detected_object_set`. The first four are mechanical -- a crop, a plane copy, a plane concatenation, a crop and an encode -- and read shorter than the OpenCV they replace, because the bridge was interleaving planes on the way in and de-interleaving them on the way out for operations that never needed either. `draw_detected_object_set` is the one with a decision in it. Its text is `font_5x7.h`'s rather than Hershey's, so the glyphs are different by construction and the `text_scale` config, which was a fraction, is rounded to a whole multiple. And it turned up a defect: the colour triple is documented "(RGB)" and defaulted to `"0 0 255"`, but went into a `cv::Scalar` over a BGR image -- so the default drew a **red** box while saying blue. The port reproduces the behaviour rather than the documentation, behaviour first, and finding 1.10 records it. Then `ocv_3frame_differencing`, which reproduces its recording exactly on both variants. It needed greyscale erosion and dilation, which `morphology.h` did not have -- it had the binary pair the VXL recordings are held to -- so `grey_erode`, `grey_dilate` and `rect_element` went in beside them. Two things in it had to be preserved rather than tidied: the unjittered path is **byte** arithmetic that saturates at 255 on the addition and clamps at 0 on the subtraction, which is part of the answer wherever the difference is bright, and `rms_over_channels` divides each squared plane by nine rather than by the plane count, which the original comment explains as keeping the scale of a mono image. Then `detect_heat_map`, the largest of them at 832 lines: a threshold, a connected-component strategy and a fixed-size strategy that box-filters the heat map and takes the maximum response. `mask_bounding_box` and `threshold_binary` are templates over the pixel type now rather than a `cv::Mat` per depth, and `get_bbox_fixed_size_dense` was deleted rather than ported -- it was unreachable, no config value selects it. Recording it first is what made it checkable: the four cases P7-T01 recorded only exercised the component strategy, so three fixed-size variants were recorded with `record.py --append` (added in this change, so a recorder can never be run against already-ported code) while OpenCV was still the implementation. Two of them then caught a real ordering bug: **`cv::findContours` returns contours in reverse raster order**, so the detections came out backwards, which no component-strategy case could show because those never had more than one. `find_contours` reverses to match and a unit test pins it. What is left is the seventh digit of the confidence, which is the box filter's mean: `cv::boxFilter` to `CV_32F` accumulates in float where `io::filter_2d` accumulates in double, so `check_detections` compares a score to 1e-6 relative and the box exactly. Then `hough_circle`, and with it `library/object_detectors` has no OpenCV left at all. The rest went to python together, per plan 2.4 and 2.6: `ocv_SIFT` and `ocv_SURF` (detection and description), `ocv_flann_based`, `estimate_homography:ocv` and `estimate_fundamental_matrix:ocv`. None of them is an `image_ops` primitive -- a scale-space pyramid, a randomised KD-tree, a RANSAC loop with a Levenberg-Marquardt refinement -- so the algorithm stays OpenCV's and only the language changes. **P7-T01 had recorded none of them**, so the first commit of this task is the recording: sixteen cases over two real frames, `detect_features` and `extract_descriptors` of the same name in one case because that is how `track_features:core` uses them and because a SIFT descriptor depends on the octave the detector found the keypoint in, which `vital::feature` cannot carry. **Three things had to be built before the port could be faithful, and all three are phase 8 work brought forward.** The generated pybind11 trampolines pass every argument to python by value, so a python implementation of `extract_descriptors` or either estimator could not fill its output parameter and would silently reduce `match_features_homography` to keeping no matches: three hand-written trampolines and an extras file for the calling side, with a convention -- return a tuple of the value then each output -- stated in `trampolines/README.md` (finding 1.18). The two estimators declare `estimate` twice and python has one name for the two, so the second overload looks for `estimate_matches` and falls back to the C++ body. And a python override cannot call its C++ base from a helper function (finding 1.19). **Four defects found, each by the recording.** `cv::Ptr::constCast` returns by value, so `detector.constCast<...>() = create( ... )` in all four SIFT and SURF wrappers assigned to a temporary: every one of their ten config keys has been ignored, and two shipped pipelines have been asking for `hessian_threshold = 5000` and getting 100. The port applies the configuration and the four affected cases are recorded as deliberate divergences. `cv::FlannBasedMatcher` is **randomised** -- KD-trees seeded from the clock, 45 or 46 matches from the same 81 descriptors twice in one process -- so its cases are contracts on agreement rather than on bytes, and the estimators are recorded on deterministic synthetic correspondences instead of on matched features. The bridge built a **BGR** mat and let cv2 take BGR2GRAY on it, which is the right luminance; handing cv2 the RGB array instead cost ten of eighty-one SIFT keypoints. And `register_using_homographies.pipe` has never configured: it names `estimate_homography:core`, which has never existed. `git grep opencv2/ -- library/{image_processing,object_detectors}` is empty, and neither directory links OpenCV or the bridge any more. **The row is marked done against that gate, and the gate is narrower than the task's own list.** The task text names `debayer`, `enhancer`, `color_correction`, `hue_shift`, `convert_color`, `split_image_habcam`, `warp_image_ocv`, `windowed_*`, `diff_of_gauss`, `canny_edge` and `detect_in_subregions_process` as well, and all of those are in `plugins/opencv`, which phase 2 has not moved into `library/` yet. They are recorded -- `tests/golden/opencv` has every one of the filters -- and unported. P7-T04b is where they go |
| P7-T04b | Port the image filters still in `plugins/opencv` | P7-T04 | done | this commit | The half of P7-T04's list that lives outside `library/`, deferred with phase 2's layout: `ocv_color_correction`, `ocv_convert_color`, `ocv_enhancer`, `ocv_debayer`, `ocv_random_hue_shift`, `ocv_optical_flow`, `split_image:habcam` and `ocv_horizontally`, `warp_image_ocv`, and the windowed chipping. Every one is already recorded, and `library/image_ops` was built for exactly these, so this is the best-covered work left in phase 7. **Three done so far**, all exact: `split_image:habcam` and `ocv_horizontally`, which were a `cv::Mat` region of interest and a clone and are `io::crop` now, and `ocv_debayer`. The debayer found a fourth defect and cost a fix to `image_ops` itself. **`ocv_debayer` swaps red and blue**: OpenCV's Bayer constants name the pattern the other way round and the filter passes its config letter straight through, so `pattern: BG` on a BG mosaic decodes as though red were at (0, 0). Measured against the source the fixture was sampled from, not argued. The port reverses the letters so it reproduces it exactly. And `image_ops::demosaic` had the wrong border rule -- it reflected where `cv::cvtColor` replicates the outermost ring from the pixel beside it -- which P7-T03's recording could not have caught, because that case was taken over a window with a two pixel margin. Then `ocv_convert_color`, which needed `rgb_to_hls` added to `image_ops` and seven more recorded pairs to be held to -- the existing case covered only the shipped rgb to hls. **Three of its colour spaces are dropped**: XYZ, YCrCb and Luv, none of which any config selects, on the same "used, nothing extra" rule that dropped `vxl_white_balancing`; asking for one is a configuration error naming the space, which an unavailable pair already was. And a fifth defect: **`cv::cvtColor`'s HSV and HLS disagree about where to wrap a negative hue** -- HSV halves then wraps and gives 0, HLS wraps then halves and gives 180, for the same pixel. One pixel of 6144 in the fixture lands there. `ocv_convert_color` is the first entry in the golden's tolerance table since phase 3: OpenCV converts an 8 bit image through fixed-point tables and `image_ops` works in double, so the two round apart on the last count -- never more than 1, 0.33 mean at worst, and two of the eight pairs exact. Then `ocv_random_hue_shift`, which is exact in the only sense it can be -- it draws from `rand()`, so the recording pins its shape and its refusal and nothing else -- and where four things had to be reproduced rather than tidied: the order and count of the draws, truncation rather than rounding on every assignment into a byte, a hue wrap at `> 180` rather than `>= 180`, and three colour shifts drawn r, g, b and applied to a **BGR** mat's channels so that the first lands on blue. Then the three refiners that had no recording at all -- recorded first, with a `refine` kind that carries each detection's **mask**, since for the two segmenters that is the entire output -- and `ocv_grabcut` and `ocv_watershed` moved to python per plan 2.6, reproducing all seven cases first run. Then `ocv_enhancer`, which thirteen pipelines select and which is the most-used OpenCV filter in the tree. **It went to python rather than to `image_ops`, and the reason is one step of it**: `cv::fastNlMeansDenoisingColored`. Non-local means is not an imgproc primitive, every shipped config disables it, and it is a DIVE toggle a user can flip -- so dropping it is a visible regression and reproducing it is a few hundred lines. In python it is one line and nothing is lost. All seventeen of its cases reproduce exactly, `vxl_enhancer` included, and that second name is now a subclass rather than a second factory: a python implementation is found by walking the interface's subclasses, and `__subclasses__` holds weak references, so an alias defined inside the registration function is collected before discovery reaches it. Then `ocv_color_correction`, which needed twelve more recorded cases first: its defaults turn all three stages off, so the one case it had said the filter leaves an image alone and nothing about the four hundred lines behind the switches. Two more defects out of that. **`water_type` does nothing** -- `set_water_type_presets()` exists and nothing calls it, so `coastal` and `turbid` are byte-identical to the default while the README documents all three presets with their numbers. And the fusion path's `c_gamma = 0.7` is ignored whenever `gamma_auto` is set, because `apply_gamma_correction` overwrites gamma from the histogram; only the combined case shows it, since gamma and fusion each agree on their own. Then six files deleted rather than ported: `adaboost_classifier`, `canny_edge_detector`, `diff_of_gauss_detector`, `gabor_features`, `hog_features` and `ellipse_proposal`, 3179 lines between them. They are **not registered anywhere** -- no factory, no process, no name in the P0 registry -- and they form a closed island: `ellipse_proposal.h` is included only by the other five and each of those only by its own source. Compiled into `viame_opencv` and reachable by nothing, which is the category P5 put `close_loops_multi_method` in. They are not `removed.json` entries, since no registered name is lost. Three of them are named in `lite-removals.md` 2.6 under open decision 6 -- `gabor_features`, `hog_features` and the adaboost family -- and the answer to that decision for these files is that there was nothing to decide. Then `add_keypoints_from_mask` -- **to C++ on `image_ops` rather than to python**, unlike everything else in this row, because `measure_objects_process` calls its `compute_keypoints` and `is_valid_keypoint_method` directly and one implementation reached from both beats two that have to agree. `measure_objects_process` came off OpenCV in the same change: it used `cv::Point2d` and a `cv::Mat` cost matrix and nothing else, so sixteen lines of row-major matrix replaced a dependency on all of OpenCV's core. It needed the PCA (a two by two symmetric eigenvector, analytic), a `cross_element` for the skeleton and a `corners()` on `rotated_rect`, and it found a **one-character defect in `image_ops`**: a minimum-area rectangle over a symmetric shape is achieved at several orientations, `cv::rotatingCalipers` keeps the last edge that achieves the minimum and `min_area_rect` kept the first. On an elliptical mask both give 462 exactly and they differ by ninety degrees, which moved the head and tail six pixels -- and those keypoints are what the stereo measurement triangulates. P7-T03's own test could not have caught it: it compared the area alone, noting the orientation was ambiguous. Then the **windowed chipping**, which is the last of this row's list and the most-used code in it: `ocv_windowed` is the detector six shipped pipelines wrap a GPU detector in, the refiner three more use, and the trainer that `train_detector_netharn_cfrnn.conf` selects. It had no recording, so seven cases were recorded first over `example_detector` -- the four modes plus uneven chips, black padding and a scale -- and the port reproduces all seven exactly. The conversion is smaller than it looks, because the chip geometry was never OpenCV's: the rectangles were already `image_rect` and the arithmetic already integers, so only three calls were `cv::` at all -- a resize, a region of interest and a zero-filled destination -- and they are `io::resize`, `io::crop` and a `padded` helper now. Two details had to be kept rather than tidied. `cv::resize` with a scale factor and no explicit size computes its destination with `saturate_cast< int >`, which **rounds**; flooring gives a chip one pixel narrower on about half of the scales, and that pixel moves every detection the chip produces. And the trainer's chip writer cloned any non-contiguous `cv::Mat` before saving, because `ocv_to_vital` only wraps refcounted memory and a region of interest would have reached the writer with a null pointer; `io::crop` allocates, so the clone is gone rather than translated. `windowed_detector`, `windowed_refiner` and `windowed_trainer` all passed their images to the bridge as `RGB_COLOR` and took them back as `RGB_COLOR`, so the round trip never swapped anything -- unlike the calibration target detector, which asked for one and took `BGR2GRAY` on it. The nested detector now gets a `simple_image_container` directly. `refine_detections_util` went in the same change, dead since grabcut and watershed moved to python. Then `detect_in_subregions_process`, the last file in this row and the last one with no recording. The four pipelines that select it are arctic-seal add-ons needing a downloaded YOLO model and two or three cameras, so the golden gained a **`process_pipeline` kind** instead: a process whose output is a detected object set rather than an image, driven through a pipeline kept in `tests/golden/pipelines/` and read back from the viame CSV it writes, in the same per-detection shape `describe_detections` produces so that `check_detections` compares both. The `pair_stereo_*` and `measure_*` processes P7-T06 still owes are in exactly that position, which is what made it worth building rather than special-casing. Six variants -- the two methods, a fixed size large enough that later centres fall inside an earlier region, the input-set passthrough, a subregion cap, and regions running off the right and bottom edges -- and the port reproduces all six. Its OpenCV was a crop and a `cv::Rect::contains`, which is what the file's own TODO said it was. Re-recording the whole opencv group with `--append` to add those cases reproduced all 103 existing ones **byte for byte**, which is a check on every port in this phase at once. **This row is done.** `ocv_color_correction` is recorded and unported. `ocv_optical_flow` turns out to be `plugins/core/optical_flow.py` already -- python, and nothing to do. `warp_image:ocv` is done: it had no recording and no python binding either, so `warp_image` joined the generated interface list first. Six of its eight cases reproduce the recording within the four counts `cv::warpPerspective`'s fixed-point interpolation costs, and four of those six are exact. The other two are declared divergences, because **the recorded alpha blend is wrong**: `1.0 - weights` in OpenCV is `Scalar( 1, 0, 0, 0 ) - weights`, so only the first channel blends and the other two compute `warped * w - dest * w` -- 80 counts of mean error against a correct blend. Latent, since the only caller never passes a mask, so the port is correct rather than bug compatible, as phase 3 decided for `vxl_threshold` |
| P7-T05 | Calibration file IO | P7-T01 | done | this commit | `library/file_io/opencv_yaml.{h,cxx}`: the FileStorage subset VIAME writes and reads -- YAML both ways, XML read only -- as a node tree, which is the shape `cv::FileStorage` has and the shape the P7-T01 recording is in. **The reader agreed with OpenCV on all five committed documents on the first run**, and the writer reproduces OpenCV's output **byte for byte** on all four YAML ones. That second half is the part worth the effort, because it is what makes a regenerated calibration file diff cleanly against the one beside it, and it took reverse-engineering two rules from what FileStorage produces rather than from its source: `icvDoubleToString` writes a value equal to its own rounding as the integer and a full stop and everything else as `%.16e`, which is why a calibration file is full of bare `0.`; and the emitter wraps a data array before a token that would end past **column 72**, continuing with seven spaces, which four sequences of differently sized numbers pin exactly -- a token ending at 72 stays and one ending at 73 moves. Three writers were converted (`calibrate_single_camera`, `calibrate_stereo_cameras`, `optimize_stereo_cameras`) and one reader: `camera_rig_io`'s hand-rolled line scanner, which read matrices and nothing else and dropped silently any whose `rows * cols` did not match its data. That boundary keeps both behaviours, since every caller below it is written around "the matrices, by name". A small pybind module, `viame.file_io._opencv_yaml`, is what lets the golden replay its `nodes` recording through the C++ reader -- this is the one case in the framework where the recorder and the replay deliberately call different code, since a file format has no registry to change implementation underneath. 24 unit tests beside it, including the byte-for-byte check and the corners no fixture has. **Not converted:** `adaboost_classifier` and `classify_fish_hierarchical_svm` also use FileStorage, for their own model indices rather than for calibration; `lite-removals.md` 2.6 leaves both to open decision 6, so they go with P7-T07 rather than being ported into code that may be deleted |
| P7-T06 | Port `measurement` to python where calib3d is needed | P7-T05 | done | this commit | Recording first, as every phase 7 task does. `tests/golden/measurement` covers the two OpenCV-backed pieces of the stereo chain that can be driven on their own -- `ocv_stereo_disparity` over eight variants and `ocv_detect_calibration_targets` over six -- on fixtures built for them: a stereo pair at two known disparities, an 8 by 6 chessboard and a 7 by 5 dot grid. The stereo pair is textured at three scales on purpose, because a matcher given a smooth gradient returns its invalid value everywhere and a recording of that says nothing; SGBM recovers both plateaus exactly, 8 and 40 pixels. **`library/measurement` is already OpenCV free** -- the task's own "done when" was satisfied before it started -- so the work is in `plugins/opencv`, which is phase 2's territory by layout and phase 7's by dependency. Two defects found while recording: `target_type` is compared against `checkerboard` and any other value, `chessboard` included, silently turns both detectors off; and the dot path ignores `target_width` and `target_height` entirely, so a board configured at the wrong grid returns the same dots. **What is still unrecorded, and blocks the port:** `calibrate_stereo_cameras`, `ocv_optimize_stereo_cameras` and the `pair_stereo_*` processes need a sequence of calibration images or a feature track set rather than one image, and the `measurement_*` pipelines need a GPU detector or input track files that are not in the tree -- `measure_via_default_fish`, the one that runs, writes zero in its length column, so there is nothing in its output to hold a port to. **The first gap is now closed:** `tests/golden/measurement` carries a synthetic stereo calibration set -- twelve views of a chessboard rendered through a rig whose parameters are known exactly, which a planar target makes exact because a plane through a pinhole is a homography -- and runs `measurement_calibrate_cameras_default.pipe` over it end to end: the target detector, the track accumulation, the calibration and the YAML writer. It is the one case in the framework with a **right answer** rather than only a previous answer, and the recorded implementation hits it to 0.3% on every focal length and on the baseline, with exactly zero distortion on a distortion-free rig. Four more defects found getting there, all in `design/lite-findings.md`: `optimize_stereo_cameras` refuses any board with an **odd** number of corners, which is its own default 7 by 5 target, and logs a warning and returns rather than failing; `-s global:key=value` never reaches `$CONFIG{global:key}`, so `-s global:square_size=30` silently calibrates at the shipped 80 and returns a baseline scaled by 80/30; OpenCV's chessboard auto-detection matches a sub-grid, so the two cameras of a stereo pair can latch onto different grids and the calibration then refuses the pair; and the calibrators fit progressively, pinning the principal point at the image centre whenever doing so does not worsen the error, which costs 2% of focal length on a decentred rig. **Two ported so far**, both exactly: `ocv_stereo_disparity` -- eight variants, reproduced on the first run -- and `ocv_detect_calibration_targets` with the four detection helpers it used from `calibrate_stereo_cameras`. Both are in `library/measurement` now rather than `plugins/opencv`, which is where the finished layout puts them. The target detector is the better test of the two: the end-to-end calibration case runs the python detector inside the C++ pipeline and still recovers the rig to 0.3%, so the port is held to the recording and to the truth at once. A third defect found porting it: it converts to grey with **red and blue swapped**, being the only caller in the tree that asks the bridge for an `RGB_COLOR` mat and then takes `BGR2GRAY` on it. **The calibration core went too**, and it is the result worth reporting: `ocv_optimize_stereo_cameras` in python, with `filter_stereo_feature_tracks` and `kmedians` beside it as `stereo_frame_selection.py`, reproduces the C++ **bit for bit** -- all eleven matrices of the rig, zero difference. That is the whole chain in python now: the target detector, the frame selection, the progressive per-camera fit, the stereo calibration, the rectification and the file writing. Two things had to be got right beyond the arithmetic. The `optimize_cameras` interface takes its cameras as an in/out parameter and returns void, so a python implementation needed the fourth hand-written trampoline; and python's `CameraMap` is bound as `simple_camera_map` with no base declared, so the trampoline casts to the concrete class and lets C++ do the upcast. The one case that is not bit exact is `frames_6`, and not because of the port: `kmedians` seeds from `cv::theRNG()`, whose state is not an input to the algorithm, and the two implementations pick six frames that differ by one. Held to a relative tolerance and to ground truth instead. **The single camera calibration went the same way.** `ocv_calibrate_single_camera` is what DIVE's "calibrate camera" button runs, and it had no recording either; a `mono_calibration` case now drives `utility_calibrate_single_camera.pipe` over the left twelve views of the same synthetic rig, so it gets both checks -- against the recorded matrices and against the truth. The python process reproduces the recording **exactly** and lands 602.20 against a true 600. It is the first **python sprokit process** phase 7 has produced rather than a python algorithm, which cost one line in the shipped pipeline: the default scheduler refuses to run a python process, so `utility_calibrate_single_camera.pipe` asks for `pythread_per_process` the way the other python-process pipelines in `configs/pipelines` do. `calibrate_single_camera.{h,cxx}` and `calibrate_single_camera_process.{h,cxx}` are deleted. Its own progressive fit differs from the stereo one in a detail worth keeping straight: the principal point test divides the horizontal offset by the width and the vertical by the **height**, where the stereo version divides both by the width. Two more defects out of it. `ocv_calibrate_single_camera` **never sees the image and guesses its size** from how far the corners reach -- 700 by 400 for a 640 by 480 frame -- and that guess is what the calibration file carries as `image_width` and `image_height`, so anything building a rectification map from the file builds it at the wrong size. And `pair_stereo_tracks_pytorch` reads `state.detection` where the binding makes it a method, so every path through it raises `AttributeError`; it cannot ever have been run. **And the measurement pipelines have a recording now**, which is what this row said it could not have. What closes the gap is a **synthetic measurement scene**, the same trick as the calibration fixture and two images long: a textured plane at a known depth, viewed through the same rig, with five segments of known length drawn on it. A plane through a pinhole is a homography, so both views are exact inverse warps of one texture and there is no rendering error to argue about; the track file the pipeline reads is generated from the exact left projections, so what is measured is the matching and the triangulation rather than an annotator's aim. Five variants, one per deterministic matching method -- `input_pairs_only`, `epipolar_template_matching`, `template_matching`, `compute_disparity`, `depth_projection` -- and between them they reach `stereoRectify`, `initUndistortRectifyMap`, `remap`, `undistortPoints`, `projectPoints` and `matchTemplate`, which is the whole OpenCV surface `measurement_utilities` has left. `feature_descriptor` is out: it goes through the randomised FLANN matcher. **The first thing it found is the largest defect of the phase**, and it is not an OpenCV one -- see the P6 row. **Three more beside it.** `compute_disparity` saturates its own disparity map, because the shipped config turns the WLS filter on and `raw` writes the filtered result back as sixteenths in an int16 where the fill values reach 32767 -- 2047 pixels of disparity on a rig whose real disparity is 45 -- and the consumer rejects only values at or below zero. It reproduces on the reference build of `main`, and the python `ocv_stereo_disparity` is bit identical to the C++ on all three WLS configurations, which is checked by a script kept in the tree rather than asserted. A right-camera track file has to be **named after the left image**, because both readers are handed camera one's file name and `read_object_track` filters on it. And `input_pairs_only` does nothing on its own: the process refuses to pair by track id, so without a `detection_pairing_method` every track is left-only and the method has nothing to take. **And `compute_measurements` is ported**, which is the largest single file of the phase: `plugins/core/measurement_utilities.cxx`, 4346 lines, is off OpenCV entirely. What it needed first was the calib3d -- `library/measurement/projection`, which is `cv::projectPoints`, `cv::undistortPoints`, `cv::stereoRectify` and `cv::initUndistortRectifyMap` checked against OpenCV on four rigs -- and after that the file is `image_ops`: `match_template_ncc` for the NCC, `remap` for the rectification, `rgb_to_gray` for the conversions, `draw_*` and `horizontal_concat` for the debug overlay, `crop` for every region of interest. The census transform and the search loops were already plain arithmetic on a `cv::Mat` and are the same arithmetic on a `vital::image`. **Three of the five recorded variants reproduce the recording exactly** -- `input_pairs_only`, `epipolar_template_matching` and `depth_projection`, none of which rectifies. `template_matching` moves by 1.4e-6, which is the float-versus-double difference `stereo_rectify` documents, and has a tolerance saying so. `compute_disparity` is declared **unstable** rather than given a tolerance: it reads its depth out of the saturated map of finding 1.21, where neighbouring pixels hold wildly different numbers, so a seventh-digit change in the rectification moves a right keypoint by ten pixels. What it is still held to is that the same tracks get a measurement and their lengths are still an order of magnitude short -- the defect, pinned, and a method that started working would fail it, as it should. **And the `pair_stereo_*` processes are ported, which finishes the row.** `pair_stereo_detections` was `projectPoints`, `undistortPoints`, `reprojectImageTo3D`, `Rodrigues`, `stereoRectify` and a `cv::Rect` intersection, so `library/measurement/projection` covers all but the last of it; `reproject_to_3d` joined that header, with the note `cv::reprojectImageTo3D` carries and every VIAME caller has ignored. Its calibration reading now goes through `plugins/core/camera_rig_io` -- the same four formats without the OpenCV types in the middle -- which let **`calibrate_stereo_cameras.{h,cxx}` and `opencv_yaml_matrix.h` be deleted**, 1131 lines and the last use of `viame::opencv::mat_to_node`. All three recorded variants reproduce within a part in ten million, which is the float-versus-double difference the rectification documents. **P7-T06 is done.** `plugins/opencv` has OpenCV in three files now: the two model classifiers of open decision 6, and `plot_metrics`, all of them P7-T07's |
| P7-T07 | Port `classifiers`, `segmentation`, `descriptors`, `training`, `evaluation` | P7-T04 | done | this commit | Most of this row's list was done under other rows as it came up: `add_keypoints_from_mask` on `image_ops`, `refine_detections_grabcut` and `refine_detections_watershed` in python, the windowed refiner and trainer, and `kmedians` with the frame selection. What is left is **five files**, and three of them are decisions rather than work. `plugins/core`'s two CSV writers turned a detection's mask into `(poly)` and `(hole)` cells with `cv::findContours` under `RETR_CCOMP` and either `cv::approxPolyDP` or `simplify_polygon`; both are **ported and byte identical** to the recording made first -- `tests/plugins/core/test_mask_polygon_csv.cxx`, six masks by five configurations, regenerated with `VIAME_RECORD_MASK_POLYGON_CSV=1`. It cost three additions to `image_ops/contours.h`: `find_borders`, which is Suzuki and Abe's border following proper rather than the `RETR_EXTERNAL` shortcut `find_contours` takes; `simplify_chain`, which is `CHAIN_APPROX_SIMPLE`; and `approx_poly`, which is Douglas-Peucker over a closed contour. **Three details had to be right and none of them is in the textbook.** The trace runs **clockwise** where `find_contours` runs the other way, and `find_contours` keeps its direction because its callers are held to what it gives. Jacob's stopping criterion fires one step late, so the start is pushed twice and has to be dropped -- not cosmetic, because it decides whether the start is the middle of a straight run that `CHAIN_APPROX_SIMPLE` removes or a corner it keeps. And OpenCV's output order is a **tree**, not the scan: siblings are prepended and the tree is walked depth first, so a top-level border comes out after the ones found later and each border's holes follow it immediately. Writing the recording also turned up that the bridge **refuses a `bool` image**, so a mask of that type reaches the writer and throws; nothing in the tree makes one. **`plugins/core` has no OpenCV left**, and neither its `find_package` nor its `VIAME_ENABLE_OPENCV` definition survives. `plot_metrics` is 1115 lines of OpenCV drawing whose replacement the plan names as python matplotlib called from `viame score` -- a rewrite with a different picture at the end, not a port, and a decision about what the picture should be. **`plot_metrics` is gone**, and it turned out to be a deletion rather than a rewrite. Upstream already ships the replacement: `tools/plot.py` is a matplotlib plotter with a `viame plot eval` subcommand that reads a directory of evaluation CSVs, and `viame score --output-plots` was already writing that directory beside its images. So scoring renders by running `viame plot eval` over the directory it just wrote, which has a property the old arrangement did not: **a user can rerun that command by hand and get the same pictures**, because the pictures come from the files rather than from a struct that existed only inside the scoring process. There is a test for exactly that. The recording made first is `tests/plugins/core/test_plot_data_export.cxx` -- not pixels, which are a different picture on purpose, but the CSV files the plots are drawn from, which is the contract between the two implementations. It found the two gaps the port had to close before it could start, both in `design/lite-findings.md`: the **track purity and continuity histograms were never exported at all**, so two of the eight pictures had nothing on disk to draw from, and the PR curve CSV **carried no average precision**, so a replotter recomputed it with eleven-point interpolation where the evaluator interpolates at every point, and the plot disagreed with the score printed beside it. Both are in the files now, and the reader falls back to recomputation only for a curve written before this. `tests/tools/test_score.py` holds the applet to the eight file names the OpenCV renderer produced, and to redrawing all eight from the directory alone. Nothing in `tools/` links OpenCV any more. Open decision 6 is answered both ways: `classify_fish_hierarchical_svm` is **removed** -- no live pipeline names it and its 1860 lines would need `cv::ml::SVM`, `cv::PCA`, `cv::HOGDescriptor`, `cv::dft` and six more between them -- and `iqr_session_adaboost` is **ported to scikit-learn**, which is the P7-T09 row |
| P7-T08 | Darknet without OpenCV | P7-T07 | done | this commit | Recorded first, on a fixture built for it: `darknet` takes its chip size from the **network**, 704 by 704, so on any of the 96 by 64 fixtures the chipping loop never runs and a `chip` recording would be a recording of nothing. `detect_scene` is 1000 by 800 with twenty-one blobs, some across a chip boundary on purpose. 24 cases over the `generic_detector` model from the yolo-generic add-on, which is installed here; an install without it, or a machine without CUDA, skips the group. **Five defects out of the recording alone**, all in `design/lite-findings.md`: a `chip_step` at or above the image's height throws an OpenCV assertion out of the region of interest; an image smaller than the network detects **nothing** in chip mode, silently, where the same frame undisabled gives six; `nms_threshold` is declared, documented and never read; `gs_to_rgb` changes nothing, because darknet's own `mat_to_image` takes `GRAY2BGR` whatever VIAME did first; and `adaptive` **latches on the first frame** -- `detect` is const and writes its choice back anyway, so the same 900 by 750 frame gives 31 detections on a fresh detector and 135 after one larger one. The port itself is smaller than the file count suggests. `darknet_custom_resize.{h,cxx}` is **deleted rather than ported**: it was a second copy of `plugins/core/windowed_utils`'s aspect-preserving fit and scale. `darknet_detector` builds the `image_t` that `Detector::detect( cv::Mat )` would have built -- resize to the network input, convert to BGR, transpose into planes over [0, 1] -- and calls the `image_t` overload, which exists whether or not the fork is built with OpenCV, then scales the boxes back the way `detect_resized` does. The trainer's bridge round trip was **two swaps that cancelled**, since it asked for a BGR matrix and handed one back, and every step between is channel order agnostic; both are gone. **All 24 cases reproduce exactly** -- every box and every confidence, through a neural network, which is a stronger statement than a pixel comparison. Getting there took two things `image_ops` did not have and neither was guessable: OpenCV's **fixed point** for an 8-bit bilinear resize, since the floating point version differs by a count on an eighth of the pixels and that is 37 detections against 35; and `resize_by_scale`, because `cv::resize` given a scale keeps sampling on the grid **that scale** defines rather than the grid its rounded output size implies -- twenty-eight counts different on the 0.704 fit, and the reason seven of the twenty-four cases failed on the first run. Both defects were in `plugins/core/windowed_utils` too, so the shipped `windowed` detector chips correctly now as well; its own recording could not have caught either, being made over `example_detector`, which returns a fixed box per image. `plugins/darknet` has no OpenCV, no bridge and no `-DOPENCV`, so the fork can be built with `ENABLE_OPENCV=OFF` without this side changing. **And `plugins/darknet` is gone**: the detector is `library/object_detectors/darknet_detector` and the trainer is `library/training/darknet_trainer`, which is where `lite-library-layout.md` line 171 always put them. `windowed_utils` moved with them, to `library/object_detectors`, because they need the chipping and `library/` must not reach up into `plugins/` -- which also starts the phase 2 merge that row 142 describes. The detector is **its own library**, `viame_object_detectors_darknet`, rather than part of `viame_object_detectors`: `libdarknet.so` drags OpenCV behind it and `viame_object_detectors` is what `plugins/core` links for the chipping, so folding it in put libdarknet and OpenCV on the load path of everything linking `viame_core` -- a gtest touching none of it failed to start for want of `libopencv_highgui`. All 24 darknet goldens and the 7 `ocv_windowed` ones still reproduce |
| P7-T09 | Switch OpenCV off | P7-T06, P7-T08 | done | this commit | **No VIAME C++ includes an OpenCV header and no VIAME library links one.** `library/opencv_bridge` is deleted -- eight files, transitional since P5-T04 (finding 1.12) -- along with the top-level `find_package( OpenCV REQUIRED )`, its version check, and the `KWIVER_OPENCV_VERSION_MAJOR` macro the imported `arrows/ocv` code branched on, which had no readers left. `VIAME_ENABLE_OPENCV` stays and now means what it had quietly come to mean: **cv2 in python**. It gates the seven `viame` applets that import it, the pipelines and examples that use them, and the wheel in the python requirements. A wheel is not a build dependency, which was the user's call. The last caller was `iqr_session_adaboost`, and it went to `sklearn.ensemble.AdaBoostClassifier` -- the user's answer to open decision 6. The session is its own library, `viame_iqr`, because it is the only thing in VIAME that calls the interpreter from C++ and `viame_core` stays free of libpython; without python it reports no model and the base class falls back to similarity, so the query still runs. **One thing is left and it is not VIAME's code**: `libdarknet.so` in the reference superbuild was built with `ENABLE_OPENCV=ON`, so `libviame_darknet.so` still pulls `libopencv_highgui`, `videoio` and `imgcodecs` transitively. VIAME's side no longer calls `detect( cv::Mat )`, so `cmake/add_project_darknet.cmake` can pass `ENABLE_OPENCV=OFF`; the flag is **not** flipped here because verifying it needs a fresh superbuild and the risk is darknet's own training binary, which loads images through OpenCV when it has it and stb otherwise |
| **Phase 8: replace the kwiver-derived infrastructure** | `phase-08-replace-kwiver-infra.md` | | | | |
| P8-T01 | Hand-written python bindings for `core_types` | P6-T06, P7-T09 | done | this commit | 66 files out of `python/kwiver/vital/types` and into `library/core_types`, each keeping its name with a `_python` suffix -- the convention `library/file_io/opencv_yaml_python.cxx` set -- because **47 of the 63 would otherwise collide** with the C++ source of the type they bind. The module path is untouched, since `kwiver_add_python_library` takes it as an argument rather than deriving it from the source location, which matters because four files outside `python/` import a submodule by name. Recorded first: `tests/golden/python_types.json` is 106 classes and 965 members, and `tests/library/core_types/test_python_types.py` checks nothing was lost and carries the behaviour a member list cannot see. Two directory-scoped settings did not travel with the files and neither failure says what it is -- see findings |
| P8-T02 | Hand-written algorithm trampolines (castxml gone) | P8-T01 | done | this commit | **The generator is deleted and its output is source.** castxml and pygccxml parsed every interface header at configure time to write these bindings; what they wrote was good -- idiomatic pybind11 with the interface docstrings carried across -- so it is committed as written rather than rewritten, which makes the change zero-risk where a rewrite would not be. Gone with it: `cpp_to_pybind11.py`, `config.ini.in`, and **130 lines of CMake** spent persuading castxml's bundled clang 13 to parse a standard library it was never going to like, plus the `KWIVER_PYBIND11_CASTXML_COMPILER` option that made a Windows build need a second Visual Studio toolset installed purely to generate python bindings. `pygccxml` and `castxml` are out of the python dependencies and out of `kwiver-setup-python`'s required list. 21 bindings were dropped as well: the generator wrote one per class in its list and the build compiled only those whose interface had been imported, so they bound nothing that exists. All 44 exported classes and their members are held to the recording |
| P8-T03 | Own plugin registry | P8-T02 | done | this commit | **Measured before starting, and the measurement reorders the task.** Loading every plugin costs 3.8 s; `viame help` already costs 0.03 s, so applet dispatch is not the problem and the fifteen-second regression the plan was written against is long gone. **2.0 s of the 3.8 is python**, and not the registration design: kwiver's `loaders.py::_findPluginModules` imports **every module in the plugin packages**, 134 of them, and four cost 1.9 s by pulling in torch. The worst is `viame.onnx.epipolar_dino_matcher` at 1.22 s -- a real algorithm whose `_DinoFeatures` derives from `nn.Module`, so torch is needed to *define* the class. Tidying imports cannot fix that; only registering by name and importing on first `create()` can, which is the manifest the task already describes. So the three parts are worth the reverse of the order they are written in: **lazy python registration is the whole runtime win**, static C++ registration is worth about a second, and folding the `viame_<name>` targets into one `libviame` is structure rather than speed. Recommend doing them in that order, and note that the fold is entangled with phases 1 and 2, both deferred. Done so far: the measurement, and one thing it found that was simply wrong -- `viame.onnx.export_stereo_mapping` is a **command line tool that registers nothing**, and its module-level `import torch` was costing 1.5 s on every plugin load in the whole of VIAME because it lives in a package the scanner walks. Deferred to `main()`; the number barely moves, because the real plugin still pulls torch, which is itself the argument for the manifest. Also found: `SPROKIT_PYTHON_MODULES` names two packages that do not exist. **Static C++ registration is done.** Each of the 38 registration files is compiled into the library it registers, with its entry point renamed by the preprocessor (`-Dregister_factories=viame_register_<lib>`) so that all of them link together -- not one of the files was edited. CMake collects the list and generates `register_builtins`, which the plugin manager calls where it used to build a directory list. `algorithms_create_plugin` builds nothing any more and `kwiver_add_plugin` builds an ordinary shared library in `lib/`, so `lib/viame/*` and `lib/kwiver/plugins` are gone. Two things the doing taught, both worth keeping: **(1)** nothing references the registry by name -- it hands itself to the manager as it loads -- and `--as-needed` is the toolchain default here, so the library was dropped from every link line and every factory with it; executables link `viame::registry`, which is the library wrapped in `-Wl,--no-as-needed`. **(2)** eager registration cost the applet fast path everything: `viame help` went 0.04 s to 2.57 s, because dispatch had suddenly started importing every python plugin. Each library now carries the `plugin_type` its module's directory used to mean, and `register_builtins` honours the same mask `load_all_plugins` takes -- `viame help` is 0.09 s and `registry-dump` 3.81 s against 3.86 s. Then the loader itself: `plugin_loader` is 530 lines down to 230 and loads nothing, the five dead filter classes are deleted, `KWIVER_PLUGIN_PATH`, `SPROKIT_MODULE_PATH` and every plugin search path are gone from the code and the setup scripts, and `PLUGIN_FILE_NAME` is now `PLUGIN_ORIGIN_LIBRARY`, which the generated registry sets -- the plan's "origin library" attribute, for free. **What is left of the task**: `SPROKIT_PYTHON_MODULES` is the last name in the done-when grep, and it only goes when the python manifest replaces it, which is P8-T10; the `libviame` fold is deferred with phases 1 and 2. **The out-of-tree hook is done** (decision 10 = keep): `external_plugins.cxx`, 150 lines and the only `dlopen` left in the tree, opens each file named in `VIAME_PLUGIN_PATH` and calls `viame_register_plugin` in it. Files, not directories -- which is the point of the decision: the scan it replaces opened everything beside a plugin and let read order decide which of two libraries defining the same factory won. Ten tests cover it against a plugin built like an out-of-tree one, including that a factory from it *runs*, that it records the path it came from, and that a missing file or a real library with no entry point costs the caller that entry and not the list. `examples/plugin_creation/cxx` and its README are the documented form. **Closed after P8-T10.** The done-when grep is empty: `SPROKIT_PYTHON_MODULES` went with the python manifest, and `plugin_loader` is now `kwiver::vital::registry` in `algorithm_framework/plugin/registry.{h,cxx}` -- 62 files, 181 occurrences, plus `get_loader()` to `get_registry()` and the dozen comments that still described a loader. A class called a loader that loads nothing sends every reader looking for the load. `lib/viame/plugins` does not exist and `algorithm_framework/registry` is 333 lines against a 1200 budget. **One divergence from `lite-build-system.md` §4, recorded in the class comment rather than left silent:** the design draws the registry in `algorithm_framework/registry/` next to the generated `register_builtins`, and the two cannot share a library -- `registry/` links every library that registers, and every library that registers calls `add_factory` on the registry, so one has to sit underneath the other. The store stays in `plugin/`. **Still deferred:** the fold of the `viame_<name>` targets into one `libviame`, which is entangled with phases 1 and 2. |
| P8-T04 | Own logger and exceptions cleanup | P8-T03 | done | this commit | **Recorded first, which here was the whole of the difficulty.** Nothing in this tree reads the log format -- DIVE does, from outside -- so there was no test to run and no way to know a rewrite had kept it except to write down what it currently is. Thirteen tests say it: a local timestamp to the millisecond, the level, the source file and line, the message; the logger's *name* nowhere in it, which is the first thing someone rewriting this assumes is there; a message with newlines becoming several lines, each with the full prefix, so a consumer reading line by line never meets a line it cannot parse. Then the rewrite. `kwiver_logger` was an abstract base with a factory behind it, and the factory was `dlopen`ed at static-initialisation time from `vital_logger_plugin` or whatever `VITAL_LOGGER_FACTORY` named, so a project could substitute log4cxx or log4cplus -- both behind build options that have never been on here. The manager, the factory and the default back end are deleted and the class is concrete: 1725 lines to 953, of which 236 are `logger.h` unchanged, because the `LOG_*` macros are the interface and they do not move. `logger_plugins` was already gone, dropped rather than moved by P5-T05. **Added**: `VIAME_LOG_LEVEL`, ahead of `KWIVER_DEFAULT_LOG_LEVEL`, which keeps working because everything written before the lite branch sets it, and a `VIAME_LOG_LEVEL` naming no level falls through rather than swallowing it; and `VIAME_LOG_FILE`, which **adds** a sink rather than replacing stderr -- diverting stderr would be a silent change for DIVE, the one consumer the task names. **Two defects fixed on the way**: the logger table was an unlocked `std::map` any thread could be first to insert into, and `location_info` was asking kwiversys for a basename `std::filesystem` gives directly, which is one fewer thing for P8-T05. Also deleted the three plugin filter classes -- commented out of the build, called by nothing, and described by the code that used to call them as never having worked |
| P8-T05 | kwiversys -> std | P8-T04 | done | this commit | **30,670 lines of vendored code gone, and the work was none of it.** `third_party/kwiversys` deletes in one commit; what took the time was the 210 call sites that had been asking it for paths, and the fact that **`std::filesystem` is not a drop-in for `kwiversys::SystemTools`**. So the replacement is a seam and not a substitution: `util/file_system.{h,cxx}` (715 lines, 26 operations) keeps kwiversys's answers, not the standard library's, and each place the two differ is marked in the source with what the difference is. Four differences, each marked in the source and recorded in the tests: `filename_path( "/a/b/c.txt/" )` is `"/a/b"` because the trailing separator is dropped before the split, where `parent_path()` reads the empty final component and answers `"/a/b/c.txt"`; `filename_last_extension( ".bashrc" )` is the whole name, where `path::extension()` is empty; `environment_path` **appends** to the caller's list and keeps the empty entries a `PATH` with a stray `:` produces; and `directory_entries` lists `.` and `..` first. `make_directory` is a fifth of the same kind -- it answers whether the directory *exists afterwards*, not whether this call created it, and a caller that reads it as "I made this" is wrong in a way no compiler catches. `util/regex.{h,cxx}` (175 lines) is the same shape over `std::regex`, keeping `find`/`match(n)` and gaining `start(n)`/`end(n)` because the token expander wants offsets; it holds its own copy of the subject string, because `std::smatch` stores iterators into the string it searched and the callers pass temporaries. **Recorded before porting**, as everywhere else in this phase: 16 file_system tests and 10 regex tests, written against kwiversys, committed green, and the swap left the test files' diffs empty. Two of the API's arguments turned out to be noise -- `find_file`'s third (`no_system_path`, `true` at all 8 sites) and `file_exists`'s second (`true` at 3 sites, meaning "and not a directory", now `file_is_regular`) -- and dropping them is most of why the call sites changed at all. Three consumers sat outside the seams: `python_script_applet` swapped `kwiversysProcess` for `posix_spawnp`+`waitpid`; `register.cxx` swapped `Encoding::ToWide` for `PyConfig_SetBytesString`, which removes a file-scope `static std::wstring` the old code needed because `config.home` wants static storage, and **fixed a real bug on the way** -- `GetModuleFileNameW( ..., sizeof( pathBuf ) )` where the argument is a count of `wchar_t`, so Windows was told the buffer was twice its size; and `token_type_sysenv` lost its `SystemInformation` member, its 18 `$SYSENV{}` tokens now reading `/proc/meminfo`, `uname`, `sysconf` and `getifaddrs` directly. Those 18 were checked one at a time against kwiversys's answers side by side before the swap, which is the only reason the odd ones stayed odd: **"virtual memory" means swap**, "available physical" is MemFree+Buffers+Cached, `osversion` is `uname -v`, and the domain name comes from reverse-resolving a non-loopback interface address rather than from the host name -- `kana.kitware.com` where the host name alone gives `kana`. The only file in the tree that writes `$SYSENV{}` is an orphaned test data file no test loads (open question 2.10). **Done-when, honestly**: `git grep kwiversys -- library tools cmake` is *not* empty -- it matches 11 lines, every one of them a comment saying which semantics were deliberately preserved and why, which is information that would be destroyed by renaming it. The checks that are empty are the ones that mean the dependency is gone: zero `#include`s, zero link-line or CMake references, nothing named `ksys` in the install tree. 375/375 tests pass. **Windows CI is not available here, so the Windows half of the done-when is untested**; the platform-specific code is `python_script_applet`'s `CreateProcessA` branch and `register.cxx`'s `GetModuleFileNameW` branch, neither compiled. |
| P8-T06 | cereal -> rapidjson helper | P8-T05 | done | this commit | **cereal's most important export was three macros.** The task reads as a swap -- replace one JSON library with another -- and two of the four call sites were already rapidjson: they included `<cereal/archives/json.hpp>` above it and called no archive at all, with a comment in each saying the include was "for the vendored rapidjson headers". That is half of it. The other half is that the header defines `RAPIDJSON_ASSERT` to throw, `RAPIDJSON_WRITE_DEFAULT_FLAGS` to `kWriteNanAndInfFlag` and `RAPIDJSON_PARSE_DEFAULT_FLAGS` to `kParseFullPrecisionFlag | kParseNanAndInfFlag` -- so those two threw on an internal assertion instead of calling `assert` (nothing, under `NDEBUG`), wrote NaN instead of refusing it, and parsed correctly-rounded. Deleting an include that looked decorative would have changed all three silently (finding 1.26). `library/file_io/json.h`, 78 lines, is where they are stated now. **It also found the split**: three more files in `plugins/core` include rapidjson directly and get the defaults, so `viame json` and the DIVE reader parse the same DIVE file to doubles that can differ by a unit in the last place. Not unified here -- that is a behaviour change, not a dependency removal -- but written down as open question 2.11 and marked at each include. **The real work was the format cereal wrote, not the one it parsed.** `feature_descriptor_io` serialises with `PortableBinaryArchive`, which the task text does not mention; nothing in this tree reads a KWFD file it did not write, and nothing outside it documents the layout, so a rewrite that changed it would have passed every test there was and made every file anyone already had unreadable. Sixteen tests say what it is, as bytes, taken before the swap: `"KWFD"`, then cereal's endianness flag **between** the magic and the version, a 16-bit version, 64-bit counts, a type byte that is computed (`is_integer << 5 | is_signed << 4 | log2( size )`) rather than assigned, and a 2x2 covariance as three numbers. `library/file_io/portable_binary.h`, 196 lines, replaces it -- as an **archive** rather than a pair of functions, so that `feature_`, `covariance_`, `rgb_color` and `vector_` keep saying what their own bytes are through the `serialize` templates they already had and the layout stays defined where it was. The `--as-needed`-shaped mistake here would have been to write the layout out by hand in one place and let it drift from the five declarations that define it. **The rig JSON**: `read_stereo_rig_json` and `write_stereo_rig_json` were real cereal archives and are rapidjson now. The reader is unchanged in behaviour down to the exception type (`std::runtime_error`, cereal's base, not vital's) and the one optional field (`k3`, and only `k3`). The writer is byte-identical, including a **negative zero** in the translation that the recording found and nothing else could have (finding 1.27). Both are covered by eight tests plus the golden calibration case -- **which is new**: JSON had been the only one of the five formats `read_stereo_rig` dispatches on with no golden fixture at all, and it is the one whose parser this task rewrites. It is also the only source with fields the reader skips, so it is the one that would catch a reader that went positional. `record.py` could not express "an existing case gains an input", and can now. **Numbers**: 13,811 lines of cereal deleted; rapidjson, 15,134 lines and 35 headers, promoted out of `cereal/external` to `third_party/rapidjson` with its `CEREAL_RAPIDJSON_*` macros renamed back to stock. `git grep cereal` matches nothing outside comments that explain what the settings used to come from. 24 new tests, all green against cereal before the swap and against rapidjson after, with the test files' diffs empty across it. |
| P8-T07 | Trim `pipeline_framework` | P8-T03 | done (target restated) | this commit | **28,458 lines to 22,063.** The target was under 15,000; see the end of this entry for why that is the wrong number and what would be needed to reach it. What is done is the part the task text describes as conditional, and the condition held: `viame pipe-check --all` across all 292 shipped pipelines finds **zero** `cluster` blocks, and the tree contains no `.cluster` file at all, so `process_cluster` and everything behind it is gone -- the cluster bakery, creator, info and splitter, `loaded_cluster`, `provided_by_cluster`, the parser's four cluster productions, the seven cluster exception classes, the cluster factory, the port-mapping pass in `pipeline.cxx` (101 references, including a two-phase resolution loop for cluster ports mapped to other cluster ports), and the python bindings for all of it. `processes_clusters` went with them: a plugin whose whole job was to scan `DEFAULT_CLUSTER_PATHS` for `.cluster` files, and **the last directory-scanning loader left after P8-T03**. Also gone: the `pipe-config` and `pipe-to-dot` applets and the 900-line `export_dot` and 400-line `export_pipe` that existed only for them -- VIAME's own `pipeline` applet already does info, get, set, flatten and check, and on the include graph rather than the flattened result -- plus `bakery_display`, which nothing had called since it was imported, and the `sync` and `thread_pool` schedulers. Eight `removed.json` entries, each with the measurement behind it. **Recorded first, and it paid twice.** The task text asks for parser and scheduler unit tests "kept and passing"; kwiver shipped about thirty of them and **not one has ever run on this branch** -- they are gated behind `KWIVER_ENABLE_PYTHON_TESTS`, which also gates three trees whose fixtures earlier phases pruned, so turning it on does not configure (open question 2.12). So ten parser tests and three scheduler tests were written instead, against the code about to be trimmed. The parser ones took three of their cases from **doc comments rather than from what the shipped pipelines exercise**, and one of the three failed: `parse_attrs` claims `attr-list ::= attr | attr ',' attr_list` three lines above itself and could not parse the second production, because it accepted the comma and then looped without fetching the token after it. `[ro,local]` had never worked, which is precisely why no pipeline uses two flags -- and "no pipeline uses it" is this task's removal criterion (finding 1.28). Fixed in one line. The scheduler tests recorded that `sync` produced byte-identical output to `thread_per_process` before `sync` was removed, which is what makes it redundant rather than merely unused. 412/412 at each step. **Port frequency is gone.** A port could declare that it produced or consumed several data per step; the pipeline solved the whole graph for a consistent set of rates and each process turned its share into the stamp increment its edges advanced by. The only two processes that ever set one were `duplicate` and `skip`, examples of the feature, and no shipped `.pipe` named either -- so every port is 1:1 now and every increment is one. With it go `rational.h`, `check_port_frequencies`, `set_core_frequency`, two exception classes and the python `PortFrequency` bindings. **Two things came out of doing it.** Writing the recording first found that `skip_process` **deadlocks** -- it declares `1 + skip` and grabs `skip`, so a pipeline containing it never returns, with no error and no output; nothing has ever run it (open question 2.13). And the removal broke every pipeline in the build, because `set_core_frequency` was also what called `make_output_stamps()`: processes then failed their own `step()` precondition. **`ctest -L BASELINE` passed anyway** -- all five tests, `registry.json` clean and all 292 pipelines resolving -- because the baseline bakes pipelines and never steps one. The three-test scheduler recording written an hour earlier caught it in seconds (finding 1.29). **Process type aliases are added**, the one item in this task that builds rather than removes: `add_process_alias` plus resolution in `create_process` -- not in the bakery as the task text says, because `create_process` covers the embedded pipeline too -- so a renamed process is a rename in the baseline rather than a removal and an addition. Five tests. **The under-15,000 target is not reachable by removing unused paths, and the measurement says so.** 22,063 lines now, and every item the task text lists is done. What remains is live: 4,331 lines of exceptions across eight files, of which only 13 of 90 classes are unreferenced anywhere; `process.cxx` at 2,224 and `process.h` at 1,427, which are the port model, the config model and the stepping protocol every process in VIAME is written against; `pipeline.cxx` at 1,824, which is the engine; 2,742 lines of adapters, which are the embedded-pipeline API DIVE uses. The three remaining unused flags -- `flag_output_const`, `flag_input_mutable`, `flag_input_static` -- are each named by exactly one *example process demonstrating that flag* and by nothing else, but each is also working machinery in the port model (`flag_input_static` is what makes `grab_from_port` fall back to a config value), so they are kept on the same reasoning as `write_stereo_rig` and `$SYSENV{}`: removing public API that works is a phase 9/10 pruning decision, not a dependency-removal one. Reaching 15,000 means **rewriting** the process base and the exception hierarchy, which is P8-T09's subject, not this one's. Recommend P8-T07 close at 22,063 with the target restated against P8-T09, rather than either being missed quietly or met by deleting working API. |
| P8-T08 | Own CMake helper implementation | P8-T07 | done | this commit | **`cmake/kwiver_compat/` is gone: 56 files, 4,492 lines, replaced by nine in `cmake/viame/`.** `git grep -i kwiver_add -- cmake library tools` matches three lines, each a comment saying what the file it is in replaced. `examples/plugin_creation` builds against the install. **Recorded first, and the recording was wrong.** The helpers decide where every file goes, and nothing checked them, so the task opens with a manifest of what VIAME installs -- and the obvious way to take one, walking the install prefix, is wrong twice. The prefix is shared with fletch, so **4,439 of the 5,584 paths it recorded were not VIAME's** (`include/cppdb`, GDAL's headers); and `make install` only ever adds, so the tree also held `lib/cmake/kwiver`, dead since P5-T05, recorded as though something produced it. That second one made the check blind in the direction the task most needed: when the eighteen kwiver CMake files stopped being installed, it reported "0 gone, 6 new". It reads `cmake --install`'s own output now -- two seconds when up to date, exact about ownership, symmetric about removals -- and the manifest is 1,786 paths that include `configs/` and `examples/`, which the directory walk had missed entirely (finding 1.30). **Four groups.** *Targets*: the branches VIAME never takes are gone (`SKBUILD`; `LIB_SUFFIX` and `library_dir` are constant, so sixty lines of relative-path arithmetic is `..`), and so are the knobs no caller turns -- `no_export`, `no_install`, `no_version`, `no_export_header`, `component` were read out of the *caller's directory scope* and nothing sets them; they are keyword arguments now. `viame_add_plugin` takes `KIND`, so 26 call sites say `PROCESSES` rather than a path whose tail P8-T03 had to pattern-match. *Tests*: one function of kwiver's five survives; the rest drove an `IMPLEMENT_TEST`-scanned-by-regex convention no VIAME test uses. The setup nearly moved every test binary -- it computes its path from `KWIVER_BINARY_DIR`, and the obvious rename to `VIAME_BINARY_DIR` would have been wrong, that being `${CMAKE_BINARY_DIR}/build`. *Python*: 417 call sites. The configure step is a copy -- **no python module VIAME installs has a `@VAR@` in it** -- and still build-time, because editing a `.py` and running `make` is expected to work. An extension module no longer goes through the general library helper; its RPATH is computed from the module path's depth rather than by subtracting two absolute paths that only happen to agree, and is byte-identical to the old one for one-level and two-level modules. One mistake worth keeping: the copies first hung off the `python` target, which is not in `ALL`, so **none of them ran** -- 4 `.py` in a build tree that needed 428, with no word from the configure or the build, because a custom command nobody asks for is not a failure. *The rest*: `kwiver-configcheck`'s eleven probes are one, and it is the only one anything reads (`ABI_DEMANGLE`); the other ten test C++11 features in a C++17 build, so a compiler failing them could not have reached the check. The sprokit macro layer -- 944 lines across ten files -- had no caller outside itself. The `VITAL_MODULE_PATH` machinery went with it: P8-T03 removed module scanning and P8-T07 removed clusters, leaving `kwiver-include-paths.h` with two of its five macros still read. **What the config package installs changed on purpose**: kwiver's thirteen files and five directories of build-internal utilities are replaced by the six helpers a plugin could actually call. `examples/plugin_creation`, the documented way to write one, uses none of either -- it calls `add_library` and `target_link_libraries( kwiver::vital )`. 409 + 9 tests pass. |
| P8-T09 | Config and algorithm base simplification | P8-T08 | done | this commit | **Final line counts, which is this task's done-when**: `core_types` 35,908, `algorithm_framework` 31,827, `pipeline_framework` 22,063 -- 89,798 in all, of which 13,922 are python bindings and trampolines (28,944 / 25,269 / 22,063 without them). **Recorded first, because "keep `config_block` semantics" needs the semantics written down.** Every pipeline, algorithm and applet reads its parameters through that class and nothing in the tree said what it does; fifteen tests do now. **Three of my first guesses were wrong**, which is the whole argument for writing them before touching anything: `set_value( key, true )` stores `"1"` and not `"true"`, so a config written by C++ and one written by hand in a `.pipe` do not look alike; `bool` accepts `true`/`yes`/`1` and `false`/`no`/`0` case-insensitively and **not** `on`/`off`, which read as an obvious pair with the rest and are the first thing anyone tidying the list would add; and the cast failure is `bad_config_block_cast_exception`, the conversion's own `bad_config_block_cast` upgraded so the message can name the key. The two worth knowing are the ones nobody would check: **`subblock` copies and `subblock_view` does not**, so writing through one reaches the parent and the other does not; and `get_value` with a default is `noexcept` and swallows a present-but-unconvertible value as readily as a missing one, so a typo in a pipeline silently gives you the default. **What came out**: `config_block_formatter` had four entry points and one caller -- `set_prefix` and `generate_source_loc` were never called from C++ or from the python bindings that exposed them, so the prefix was always empty and the source location always printed, and `print` branched on constants; `format_block` was declared private and never defined, so a caller would have got a link error rather than an answer. Its output is byte-identical, which two recordings taken first say. Three macros in `pluggable_macro_magic.h` were referenced by nothing including itself -- `KWIVER_STRINGIFY`, `MAYBE_COMMENT`, and a `TEST_OPT_ARG` self-test of the variadic machinery that also put a file-scope `static int _test_opt_arg` in a header, one per translation unit, four hundred of them. **What stayed, and why**: `algorithm_capabilities` is used by `video_input_image_list` and `video_input_process`, so the task's "if unused" does not apply; `format_config_block` is an extension point whose own comment says it is for downstream tools, which is the call already made for `write_stereo_rig` and `$SYSENV{}`; `config_block`'s `get_enum_value`, `get_value_as_vector` and `get_name` have no in-tree caller and stay for the same reason, the task's instruction being to keep the semantics. `algorithm.txx` is untouched: all six of its entry points are used, 44 to 47 times each for the three `*_nested_algo_configuration` helpers. **A defect found and not fixed**: `close_loops:appearance_indexed` is registered, is in the baseline, and requires a nested `match_descriptor_sets` -- **the only one of the 45 interfaces in `algo/` with no registered implementation** -- so a pipeline selecting it could not configure. No shipped pipeline names it, so nothing has tried. That is the second instance of open question 2.13's shape in two tasks, and 2.13 now says what the check that would close it looks like. **One thing I broke and fixed**: `baseline:install` re-runs the install, which rewrites the prefix every other test reads through `setup_viame.sh`, and `baseline:pipes` failed once under `-j3` because of it; it is `RUN_SERIAL` now. 424 + 9 tests pass. |
| P8-T10 | Lazy python registration and startup budget | P8-T03 | done | this commit | **`viame runner --help` 2.72 s to 0.39 s, `registry-dump` 4.01 s to 0.69 s, both under budget.** Every python package now names what it provides instead of importing it: `__vital_algorithm_declarations__` is `( interface, name, description, "module:Class" )` and `__sprokit_process_declarations__` is the same without the interface, and `discovery.py` turns each into a stand-in -- a type for an algorithm, a closure for a process ctor -- that imports its module the first time something asks for an instance. Eleven packages converted, 74 algorithms and 21 processes. `module_loader` no longer scans a package that declares. **Two things the doing taught, both recorded as findings.** (1.31) The import being replaced did more than what it was for: `register_vital_algorithm`, which attaches `from_config`, `get_default_config` and `plugin_name` to an implementation class, ran as a side effect of the scan importing the module, so the first lazy version handed the factory a class missing all three -- registered fine, dumped fine, `AttributeError` at first `create()`. Caught by a golden recording that builds `srnn_tracker`, not by the baseline, which listed the name and moved on. (1.32) The package list lived in `setup_viame.sh`, and **four of its sixteen names were packages this tree does not install under any option**; a missing package is skipped at debug level, so the list failed open and had been wrong for years, with `registry-dump` reporting the variable's contents as fact. The list is now `BUILTIN_PLUGIN_PACKAGES` in `discovery.py` next to the code that reads it, `VIAME_PYTHON_PLUGINS` carries add-ons and nothing else, `SPROKIT_PYTHON_MODULES` is gone from the tree (which closes P8-T02's done-when grep), and the dump reports what actually loaded. Also removed: `get_python_plugins_from_entrypoint` read the same entry points through `pkg_resources`, 110 ms of import on every command -- a fifth of the budget -- to look for a hook nothing uses; it reads them through `importlib.metadata` now, which `discovery.py` was already using for the other group. **Two tests, because the claim can fail two ways.** `startup:budget` is the clock the task asks for (best of three runs, alone, since the budget is a claim about work and a loaded machine only adds noise upward). `startup:imports` reads `PYTHONPROFILEIMPORTTIME` and asserts what was imported, which fails identically on every machine: nothing heavy in `viame runner --help`, and no `torch` or `mmdet` in `registry-dump`. `cv2` is expected in the dump and allowed there -- a process's ports and config keys do not exist until it is constructed, and `gmm_motion_detector` declares its keys from an OpenCV algorithm's `default_params()`, so listing it means importing it. The task names `viame --version`, which is not a flag this tree has and never was; `viame help`, the same fast path, is measured in its place. |
| **Phase 9: python packaging** | `phase-09-python-packaging.md` | | | | |
| P9-T01 | Wheel CI | P5-T07 (can run in parallel with P6-P8) | todo | | |
| P9-T02 | Vendored python -> wheels | P9-T01 | todo | | |
| P9-T03 | Remove submodules and install-time patches | P9-T02 | todo | | |
| P9-T04 | Lock file hygiene | P9-T03 | todo | | |
| **Phase 10: install layout, platforms, packaging** | `phase-10-install-platforms.md` | | | | |
| P10-T01 | Install layout and setup script | P8-T09 | todo | | |
| P10-T02 | CMakePresets | P10-T01 | todo | | |
| P10-T03 | Windows | P10-T02 | todo | | |
| P10-T04 | macOS | P10-T02 | todo | | |
| P10-T05 | Docker images and release packaging | P10-T02 | todo | | |
| P10-T06 | Release notes and migration guide | P10-T05 | todo | | |
| **Phase 11 (optional): rename to viame** | `phase-11-rename.md` | | | | |
| P11-T01 | C++ namespace rename | P10-T06 | todo | | |
| P11-T02 | Python module rename | P11-T01 | todo | | |
| P11-T02b | Dissolve `python/` into `library/` | P11-T02 | todo | | Asked for during P2-T04: the top-level `python/` directory should not exist, the bindings belong beside the C++ they bind. Blocked on the package rename, which is what makes the two trees one |
| P11-T03 | Env var and log-level rename | P11-T02 | todo | | |

## Decisions taken

| Date | Decision | Chosen by | Where recorded |
|---|---|---|---|
| 2026-09-09 | Open decision 1: PyPI wheels stay acceptable, so the phase 4 PyAV reader and the phase 7 `opencv-python-headless` fallback are both in scope. The user's rule is that every dependency not on PyPI gets merged into `library/` | user | STATUS.md, lite-plan.md section 7 |
| 2026-09-09 | Open decision 3: drop `kw_archive_writer`. It was the last VXL user in kwiver's sprokit processes and keeping it meant reproducing VXL's `vsl` binary serialisation byte for byte | user | tests/baseline/removed.json, STATUS.md P3-T07 |
| 2026-09-09 | Converted code goes straight into `library/` per `lite-library-layout.md` rather than into new `plugins/` directories to be moved in phase 2 | user | library/, STATUS.md P3-T07 |
| 2026-09-09 | Follow the design documents rather than inventing implementations: the `vidl_ffmpeg` C++ wrapper was dropped in favour of the alias the plan specifies, with the gap recorded in `pending.json` | user | STATUS.md P3-T12 |
| 2026-09-09 | Phases 0 and 1 build only the VIAME project against the reference `main` superbuild's fletch/kwiver rather than rebuilding the superbuild; phase 1 replaces the arrangement | agent, build cost | STATUS.md "Build arrangement" |
| 2026-09-09 | `pipe-check --all` reads the install tree rather than extracting add-on zips | agent, P0-T03 | STATUS.md P0-T03 note |
| 2026-09-09 | Baseline is a CUDA build only; the CPU union is deferred to P0-T06 | agent, P0-T04 | STATUS.md P0-T04 note |
| 2026-09-09 | Dependency removal comes before the restructuring: phase 3 onward runs against the existing `plugins/` tree, and phases 1 and 2 move the resulting code afterwards. Neither phase 1 nor phase 2 removes a dependency, and both are large; going at the dependencies first gets the reduction sooner at the cost of the layout move carrying a little more | user | STATUS.md, this row |
| 2026-09-09 | `vidl_ffmpeg` is used, by VIAME's own tooling rather than by any shipped pipeline, and cannot be aliased to the `ffmpeg` reader: no shared config keys. P3-T12 writes a bridge | agent, P3-T07 | tasks/phase-03 P3-T12 |
| 2026-09-09 | `vxl` image_io is not aliasable to `ocv`, contrary to lite-removals.md §1: `ocv` declares no config keys at all, and 56 pipelines set `force_byte`, one of the two options whose behaviour actually differs. P3-T06 writes a `core` image_io in VIAME keeping all five keys instead | agent, P3-T06 | tasks/phase-03 P3-T06 |
| 2026-09-09 | `vxl_white_balancing` is removed rather than ported: zero pipeline uses, 682 lines of VXL | agent, P3-T05 | tests/baseline/removed.json |
| 2026-09-09 | `arrows/vxl` is what registers `vxl_convert_image` and the rest, so a replacement cannot claim the old name until kwiver is built with `KWIVER_ENABLE_VXL=OFF`. That flag is already wired to `VIAME_ENABLE_VXL` in `add_project_kwiver.cmake`, so nothing new is needed in the build; P3-T10 only exists because this tree had been reusing the reference superbuild's kwiver rather than building its own. Corrected after the first framing of P3-T10 wrongly called kwiver "prebuilt" and treated it as a blocker | user | STATUS.md P3-T03 note, tasks/phase-03 P3-T10 |
| 2026-09-09 | `av` is declared as a VIAME dependency rather than arriving as a transitive requirement of `pytorchvideo`, and the install was moved from the source build of `av` 10, which linked fletch's FFmpeg, to the wheel, which carries its own. That is what makes turning FFmpeg off in fletch and kwiver leave a working video stack | agent, P4-T05 | cmake/add_project_python_deps.cmake |
| 2026-09-09 | Python modules live beside the C++ of the library they belong to rather than in a `library/<dir>/python/` subdirectory. `library/video_io/python/` was flattened and the layout, build system, agent guide and phase 2, 4, 5, 6 and 8 task documents updated to match | user | design/lite-library-layout.md, design/AGENT_GUIDE.md |
| 2026-09-11 | Open decision 2, half of it: PostgreSQL support is removed entirely rather than kept optional. Upstream 0cdefae0f had already made the search index per-video files with PostgreSQL as one backend, so this removes a backend rather than a feature | user | plugins/cppdb deleted, tests/baseline/removed.json |
| 2026-09-11 | Open decision 1: `plot_metrics` goes to python matplotlib, as `lite-removals.md` 2.6 says. Linking against matplotlib from a `.py` is acceptable | user | STATUS.md "Decisions needed" |
| 2026-09-11 | ZLib becomes a vendored miniz, stripped to deflate, inflate and crc32; darknet is vendored inference only and its trainer removed with the executable it shelled out to; GoogleTest is fetched and built when tests are enabled, so that fletch can go entirely | user | third_party/, tests/baseline/removed.json |
| 2026-09-12 | Open decision 10: **keep** the out-of-tree C++ plugin hook, as an explicit list of files rather than a directory scan, but it is not critical and goes last in P8-T03 | user | design/lite-build-system.md 4, tasks/phase-08 P8-T03 |
| 2026-09-12 | P8-T05's done-when grep (`git grep kwiversys` empty) is read as "no code, build or link reference" rather than "no occurrence of the string": the 11 remaining matches are comments recording which semantics were deliberately preserved, and renaming the library out of them would destroy the reason they exist | claude | design/lite-findings.md 1.24, tasks/phase-08 P8-T05 |
| 2026-09-12 | `$SYSENV{}` kept and reimplemented in P8-T05 rather than deleted, despite having no live caller: deleting a documented config feature belongs in P8-T09, not in a dependency-removal task | claude | design/lite-findings.md 2.10 |
| 2026-09-12 | P8-T06 keeps the JSON split rather than unifying it: three files in `plugins/core` parse with rapidjson's defaults and two with cereal's full-precision settings, so `viame json` and the DIVE reader can disagree by a ULP. Making five readers round differently is a behaviour change, not a dependency removal; the settings are stated and the question is recorded instead | claude | design/lite-findings.md 2.11 |
| 2026-09-12 | `write_stereo_rig` ported rather than deleted, though nothing calls it: it is exported and declared in a public header, and removing public API is a phase 9/10 pruning decision. Its format is recorded first, so the pruning decision has something to point at | claude | design/lite-findings.md 1.27 |
| 2026-09-13 | Open decision 4: **drop the Matlab bridge.** `plugins/matlab` is deleted with `VIAME_ENABLE_MATLAB` and the one add-on pipeline that selected a `matlab` detector. It registered nothing and installed nothing: the `matlab` image_object_detector the task text expected to record in `removed.json` came from kwiver's `arrows/matlab`, which P5 had already dropped | user | STATUS.md P1-T08, plugins/matlab deleted |
| 2026-09-13 | DIVE stays its own repository, referenced as a git submodule, and is not integrated into VIAME. VIAME may build it or fetch its release, but `packages/dive` is a pointer to Kitware/dive and nothing of DIVE's source is carried here | user | STATUS.md P1-T06 |
| 2026-09-13 | Fletch is deleted. Nothing VIAME builds, installs or runs takes anything from it -- shown by building a complete VIAME into a prefix fletch had never touched and running it, where the only things missing were python packages | claude, on measurement | STATUS.md, the commit |
| 2026-09-13 | cv2 comes from PyPI, `opencv-contrib-python-headless`, unpinned -- 5.0.0.93 today. contrib rather than plain because `ximgproc` and `xfeatures2d` are used and the plain wheel has neither; latest rather than 4.x because 5.0's golden differences are a strict subset of 4.14's. Accepts eleven golden tolerances at the measured last-bit magnitudes, and loses SURF, which no wheel is built with | user | STATUS.md P1-T08, tests/golden/test_golden.py TOLERANCES |
| 2026-09-11 | Vendor minimally: only the files VIAME compiles or includes, never a distribution. darknet is to be vendored; pybind11 is vendored as the headers actually reached; GTest stays a found dependency, because only tests link it and tests are not in a release | user | third_party/README.md, design/lite-dependencies.md |
| 2026-09-11 | Track upstream: `viame/main` is merged into `lite` rather than the branch staying at its fork point, so that phase 7 ports the current code | user | the merge commit |
| 2026-09-09 | Golden coverage is the full set: per filter fixtures for every config variant the shipped pipelines use, plus whole pipeline recordings, committed under `tests/golden/` | user | tests/golden/README.md |

## Decisions needed to finish phase 1

**None.** The cv2 question is settled: `opencv-contrib-python-headless`,
unpinned, which is 5.0.0.93 today. See the row below and P1-T07.

## Decisions taken in phase 7

**None outstanding. Phase 7 is done.**

All four of the original questions are answered:
PostgreSQL is removed, `plot_metrics` went to matplotlib, `image_viewer` is
python, and open decision 6 is settled both ways --
`classify_fish_hierarchical_svm` removed, `iqr_session_adaboost` ported to
scikit-learn.

No VIAME C++ includes an OpenCV header and no VIAME library links one.
`VIAME_ENABLE_OPENCV` now means cv2 in python: the seven applets that import
it, the pipelines and examples that use them, and the wheel.

The one item that was outstanding -- darknet in the reference superbuild was
built with `ENABLE_OPENCV=ON`, so `libviame_darknet.so` pulled
`libopencv_highgui`, `videoio` and `imgcodecs` through `libdarknet.so` --
**is closed, and not by passing the flag.** There is no external darknet
build to pass it to: darknet is `third_party/darknet`, compiled with the
rest of the tree into a static `libdarknet.a` with `OPENCV` undefined.
Checked on the finished install: **not one VIAME library, and not one python
extension, has a `NEEDED` entry for any `libopencv`.**

## Removed names log

Mirror of `tests/baseline/removed.json` with the task that removed each entry.

| Kind | Interface | Name | Task | Reason |
|---|---|---|---|---|
| algorithm | various | 29 `arrows/core`, 24 `arrows/ocv`, 2 `arrows/mvg` registrations | P5-T04 | No shipped pipeline, config or test selects any of them, and nothing in VIAME's own code names one. The `ocv_*` twenty are OpenCV's feature detectors, descriptors and matchers other than the three VIAME uses. `example_detector` was in this set until four pytorch tests went red: they build a pipeline in their own source |
| process | | 18 `sprokit/processes/core`, 5 `sprokit/processes/flow` | P5-T04 | No shipped pipeline, config or test instantiates any of them. `frame_list_input` was in this set for the same reason as `example_detector` |
| algorithm | bundle_adjust, estimate_canonical_transform, estimate_essential_matrix, estimate_fundamental_matrix, estimate_homography, estimate_similarity_transform, match_features, nearest_neighbors, optimize_cameras, split_image, triangulate_landmarks | `vxl`, `vxl_plane`, `vxl_constrained`, `vxl_kd_tree` | P3-T06 | No shipped pipeline or config selects any of them |
| algorithm | image_filter | `vxl_aligned_edge_detection`, `vxl_high_pass_filter`, `vxl_hashed_image_classifier_filter`, `vxl_pixel_feature_extractor` | P3-T06 | No shipped pipeline or config selects any of them |
| algorithm | image_filter | `vxl_white_balancing` | P3-T05 | Zero pipeline uses, 682 lines of VXL |
| process | | `kw_archive_writer` | P3-T07 | Open decision 3: the last VXL user in sprokit's processes, and keeping it meant reproducing VXL's `vsl` binary serialisation byte for byte |
| algorithm | kwiver_applet | `compare-klv` | P5-T02 | arrows/klv's applet, which goes with the arrow |
| algorithm | kwiver_applet | `dump-klv`, `transcode`, `bundle-adjust-tool`, `init-cameras-landmarks`, `track-features` | P5-T04 | arrows/core's and arrows/mvg's applets. Each was already dead: `dump-klv` needs the KLV arrow P5-T02 turned off, `transcode` needs the FFmpeg phase 4 removed, and the three mvg applets drive algorithms P5-T01 removed for having no user |
| algorithm | metadata_filter, buffered_metadata_filter | `apply_child_klv`, `update_klv` | P5-T01 | arrows/klv, unused; the only reason the closure reached it |
| algorithm | metadata_filter | `derive_corner_points` | P5-T01 | arrows/geocalc, unused; the only reason the closure reached it |
| algorithm | integrate_depth_maps | `cuda` | P5-T01 | arrows/cuda, unused; the only reason the closure reached it |
| process | | `template` | P5-T01 | sprokit's worked example of writing a process |
| process | | `file_transport_send` | P5-T01 | sprokit/processes/transport, unused, and its zmq siblings are not even registered here |
| algorithm | video_input | `ffmpeg_clip` | P4-T05 | arrows/ffmpeg's clip reader; no pipeline, config or tool asks for it |
| algorithm | image_io | `ffmpeg` | P4-T05 | arrows/ffmpeg's still-image reader; no pipeline or config selects it, and `core`, `ocv` and `vxl` all remain |

## Usage scan: what arrows/vxl registers, and what the pipelines touch

From the 293 installed `.pipe` and `.conf` files, comments excluded (P3-T10).
Only the first group moves into VIAME; the second goes to `removed.json`.

| Registered name | Interface | Pipeline uses | Disposition |
|---|---|---|---:|
| `vxl_convert_image` | image_filter | 255 | ported, P3-T03 |
| `vxl` | image_io (reader and writer only) | 116 | alias to `ocv`, P3-T06 |
| `vxl_average` | image_filter | 66 | P3-T04 |
| `vxl_color_commonality` | image_filter | 6 | P3-T04 |
| `vxl_morphology` | image_filter | 8 | P3-T04 |
| `vxl_threshold` | image_filter | 3 | P3-T04 |
| `vxl_homography_guided` | close_loops | 2 | P3-T11 (plan gap) |
| `vxl_enhancer`, `vxl_white_balancing` | image_filter | 0 | VIAME's own, still port off VXL, P3-T05 |
| `format_images_srm` | process | 3 | VIAME's own, P3-T05 |
| `kw_archive_writer` | process | 6 | open decision 3 |
| `vxl` | bundle_adjust, estimate_essential_matrix, estimate_fundamental_matrix, estimate_homography, estimate_similarity_transform, optimize_cameras, split_image, triangulate_landmarks | 0 | `removed.json` |
| `vxl_plane` | estimate_canonical_transform | 0 | `removed.json` |
| `vxl_constrained` | match_features | 0 | `removed.json` |
| `vxl_kd_tree` | nearest_neighbors | 0 | `removed.json` |
| `vxl_aligned_edge_detection`, `vxl_high_pass_filter`, `vxl_hashed_image_classifier_filter`, `vxl_pixel_feature_extractor` | image_filter | 0 | `removed.json` |

The bare name `vxl` is registered for nine interfaces but only ever selected as
an `image_reader` or `image_writer` type, so only the image_io needs to survive.

## A finding worth carrying into later phases

Building a `vital::image` from an interleaved numpy array costs 14 ms for a
1080p RGB frame, against 0.5 ms for the same data laid out the way vital
stores it. `new_image_from_numpy` wraps the buffer and calls
`image::copy_from`, which only memcpys when the source and destination strides
agree; vital's default is planar (`w_step` 1, `h_step` width, `d_step`
width * height) while numpy's RGB is interleaved, so every such conversion
walks pixel by pixel at roughly 390 MB/s.

This is not specific to video. Every python implementation in VIAME that
returns an image built from a numpy array pays it, which includes the pytorch
detectors and the segmenters. Phase 5 imports this code, and phase 8 rewrites
the bindings; either is a chance to make the interleaved case fast rather than
asking every caller to know about the layout.

## Where phase 7's OpenCV removal stands

Every `#include <opencv2/` left in the tree as of P7-T09, and what each is
waiting for. Seventeen files, of which eight are the bridge. **`library/` is
down to the bridge itself.**

`library/examples/template_process.cxx` came off this list without needing
P7-T09's decision after all. It showed `cv::Mat` in and out because that was
what an author of a new process wanted; now that every process in the tree
takes `vital::image` and reaches for `image_ops`, showing `cv::Mat` is
telling a new author to do what nothing else does. The template shows the
plain thing instead, and says where `dispatch_pixel_type` comes in for a
process that takes whatever pixel type arrives.

| File | Why it is still there |
|---|---|
| `tests/plugins/core/test_tracks_pairing_from_stereo.cxx` | Not built -- its `kwiver_discover_gtests` line is commented out, waiting on `tracks_pairing_from_stereo.h` |

`plugins/seagis/seagis_measurement_process.cxx` came off this list in P7-T07
without being compiled: `VIAME_ENABLE_SEAGIS` is off and needs a proprietary
library, so the change was checked by reading. It is the same two lines its
callers had.

Four files came off this list in P7-T06 for a reason worth knowing: their
OpenCV was inside `#if defined DEBUG`, which no build has ever defined. Dead
code that made `library/video_io` look like it needed OpenCV when it did not.

## Measurements

The 1080p decode figure is measured through the python bindings, decode plus
`frame_image` with no pixel conversion, best of three passes. The plan assumed
a 150 fps reference; the measured number is what the P4-T02 gate of 60 per
cent is taken against, so that gate is 58 fps.

| What | Value | Task | Date |
|---|---|---|---|
| C++ video decode throughput, 1080p h264 (baseline) | 95.9 fps | P4-T01 | 2026-09-09 |
| `ffmpeg_cli` decode throughput | 65.3 fps (1080p h264, same clip and method; 69 per cent of the C++ reader measured at 94.1 fps in the same run, with PyAV at 89.6) | P4-T04 | 2026-09-09 |
| PyAV decode throughput | 82.4 fps (1080p h264, same clip and method as the baseline; 83 per cent of the C++ reader measured at 99.1 fps in the same run) | P4-T02 | 2026-09-09 |
| kwiver files copied (lines) | 876 files reachable, 157,857 lines, listed in `design/lite-kwiver-files.txt`; the task expected 120k to 150k | P5-T01 | 2026-09-10 |
| core_types + algorithm_framework lines after prune | 67,810 (35,972 + 31,838), counting `.h`, `.cxx`, `.txx` and `.py` | P5-T06 | 2026-09-13 |
| pipeline_framework lines after trim | 22,063 in 88 files | P8-T07 | 2026-09-13 |
| `viame help` / `runner --help` / `registry-dump` startup, before and after lazy python registration | 0.12 / 2.72 / 4.01 s before; **0.11 / 0.39 / 0.69 s** after (best of three, warm, idle machine; budgets 0.5 / 0.5 / 2.0). `viame --version` does not exist, so `viame help` stands for the dispatch fast path | P8-T10 | 2026-09-13 |
| Install size, `main` baseline (see lite-install-size.md) | 14 GB | - | 2026-09-08 |
| Install size after P1 lock files + blacklist | | P1-T08 | |
| Install size at end of P10 | | P10-T05 | |
| final core_types / algorithm_framework / pipeline_framework lines | 35,972 / 31,838 / 22,063 = 89,873, against 137,764 for the whole of `library/` | P8-T09 | 2026-09-13 |
