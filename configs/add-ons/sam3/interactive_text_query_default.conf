# =============================================================================
# Interactive Text Query Service Default Configuration (SAM3 add-on)
#
# Overrides the core VIAME interactive_text_query_default.conf stub with the
# SAM3 text query backend when the SAM3 add-on is installed.
# =============================================================================

include interactive_text_query_sam3.conf

# -- end of file --
