# =============================================================================
# Title: Interactive Stereo - Default Configuration
#
# Description: Default interactive stereo configuration for the
# fast-fdn-stereo add-on: Fast-Foundation-Stereo at full service scale.
# backend = auto uses the shipped TensorRT engine when a TensorRT runtime is
# installed (~0.2 s/frame on an RTX 4090, 5 GB) and onnxruntime-CUDA
# otherwise (~0.3-0.5 s/frame, 3 GB).
# =============================================================================

# ---- Default Implementation --------------------------------------------------

include interactive_stereo_fast_fdn_stereo.conf
