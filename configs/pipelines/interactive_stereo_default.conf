# =============================================================================
# Title: Interactive Stereo Service Configuration (Default)
#
# Description: KWIVER flat config for the interactive stereo annotation service.
# Used by python -m viame.core.interactive_stereo for interactive disparity
# computation in DIVE stereo annotation mode.
#
# This file includes the template configuration which uses non-ML methods
# (OpenCV SGBM) for stereo matching. To use ML-based methods (Foundation-Stereo),
# create a custom config or modify the include below.
# =============================================================================

# Include the template configuration (OpenCV SGBM - non-ML methods)
include interactive_stereo_template.conf
