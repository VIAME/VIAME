FROM kitware/viame:gpu-algorithms-web

RUN apt-get update -y \
  && apt-get install -y unzip \
  && rm -rf /var/lib/apt/lists/* \
  && /opt/noaa/viame/bin/download_viame_addons.sh DEFAULT-FISH GENERIC
