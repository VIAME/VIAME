# =============================================================================
# Title: Uses the track_objects process with ByteTrack algorithm which works well on CPU
#
# Description: Uses the track_objects process with ByteTrack algorithm which
# works well on CPU as it doesn't require deep feature extraction.
# =============================================================================

process tracker
  :: track_objects
  :track_objects:type                  bytetrack

block track_objects:bytetrack
  :high_thresh                         0.6
  :low_thresh                          0.1
  :match_thresh                        0.8
  :track_buffer                        30
  :new_track_thresh                    0.6
endblock

# -- end of file --
