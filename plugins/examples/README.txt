This directory contains simple image filter and object detector implementations
which only print out "Hello World" or user specified text. They are meant as
basic examples, alongside those stored in the templates folder. Both C++ and
python examples are provided in this directory, which do the same exact thing.
