
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  jobs:
    pre_build:
      - python docs/manual/dive_docs.py

sphinx:
  configuration: docs/manual/conf.py

python:
  install:
    - requirements: docs/manual/requirements.txt
