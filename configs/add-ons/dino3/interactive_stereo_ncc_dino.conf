# Interactive stereo point matching: NCC template matching along the epipolar
# line, with DINO features pre-selecting the candidates (requires the DINO
# add-on's weights and a GPU).

include interactive_stereo_template.conf

dino_model_name = dinov2_vitb14
dino_top_k = 25
relativepath dino_weights_path = models/dinov2_vitb14.pth
