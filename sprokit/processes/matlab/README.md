This directory contains an example process that interfaces to a matlab
program. The details of configuring the matlab program and converting
the process inputs to matlab values depends on what ports are
declared.
