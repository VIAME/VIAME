# =============================================================================
# Title: Interactive Stereo Service Configuration (Default)
#
# Description: Default interactive stereo configuration using epipolar template
# matching. This uses the same matching approach as
# measurement_from_annotations_template.pipe.
#
# When the fdn-stereo add-on is installed, this file is overridden to use
# Foundation-Stereo dense disparity instead.
# =============================================================================

# Include the template configuration (epipolar template matching - no GPU needed)
include interactive_stereo_template.conf
