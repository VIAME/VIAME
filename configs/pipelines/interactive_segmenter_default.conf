# =============================================================================
# Default Interactive Segmentation Service Configuration
#
# Selects the default point-segmentation backend. The core distribution uses
# the OpenCV watershed segmenter, which needs no model or GPU; model add-ons
# override this file to swap in their own backend.
# =============================================================================

include interactive_segmenter_watershed.conf

# -- end of file --
